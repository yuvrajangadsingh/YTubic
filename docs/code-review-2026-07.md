# Review execution plan (in batches, easy on the limits)

## Why the last run burned through the limits and how to avoid it

The last workflow spun up ~98 agents in one go: 9 domain reviewers **plus** verification, where every high finding was checked through 3 "lenses" (×3 agents) and every medium got one more agent on top. The result was an avalanche.

**Rules for the new mode:**
1. **Don't redo the review.** 9 reviewers have already run, 72 findings sit below in this file. That is a finished result.
2. **1 agent per finding**, one "refute" lens instead of three. This is the main lever: it drops the ×3.
3. **Hard cap of ≤ 8 agents per workflow run.** Every step below already fits that limit.
4. **I do the fixes myself** (Edit/Write, no fan-out of agents). Agents are only for independent verification.
5. **2-3 steps per session.** Peak spend per session is ~16-24 agents instead of 98.

## Steps

Cost legend: 🤖N is how many agents the step spins up.

**Track A, verification (drop the false ones, pin down severity and the fix):**
- [x] **Step 1: Verify HIGH #1-6** ✅ done. Security/perf/player core.
- [x] **Step 2: Verify HIGH #7-12** ✅ done. Cross-window/cache/data/infra.
- [x] **Step 3: Verify MEDIUM, Rust + player** ✅ done (together with 4-5, in one 24-agent run).
- [x] **Step 4: Verify MEDIUM, windows/layout + query keys** ✅ done.
- [x] **Step 5: Verify MEDIUM, lyrics + UI/lists + duplicates** ✅ done.
- [x] **Step 6: LOW, triage without agents** ✅ done. 36 low marked up (25 confirmed, 11 light triage, 0 false, 4 candidates for medium).
- [x] **Step 7: Final report** ✅ done, see the "📋 Final report" section below. **Track A (verification) is fully finished.**

**Track B, fixes (optional, on your call, once the relevant group is verified):**
- [x] **Step 8: Quick HIGH wins** ✅ done (2026-07-03). git init + baseline commit, branch `fix/review-p0`; fixed #3, #4, #5, #6, #9, #10 + the persist key (low). `tsc` and `cargo check` green. Did NOT touch the blocking resolve (dead code, deferred).
- [x] **Step 9: Cross-window/cache HIGH + security** ✅ done (2026-07-03). #8 (floating window without persist), #7 (bridge actions), SSRF medium #1 (cache_cover allowlist), #1 (per-launch token on the proxy). 2 commits (branch `fix/review-p0` now has 3 fix commits). `tsc` + `cargo check` green. ⚠️ #1 (proxy token) changes streaming routing, so it needs a runtime smoke test. Dead youtubei.js deferred to P4/Step 11.
- [x] **Step 10: MEDIUM fixes** ✅ done (2026-07-03). 19/24 medium fixed; 5 deferred (see below).
- [x] **Step 11: LOW cleanup** ✅ done (2026-07-03). ~16 low fixed (dead code, debris, README, regex, a11y, cookies, …); the rest deferred.
- [x] **Step 12: Final validation** ✅ done (2026-07-03). `pnpm build` (tsc+vite) exit 0, `cargo check` exit 0. youtubei.js confirmed out of the bundle (settings chunk 277KB→12KB).

## Progress

**Steps 1-5 done (2026-07-03).**
- HIGH (12): **11 confirmed**, 1 reclassified as low (#2, dead code, the freeze is unreachable). 0 rejected.
- MEDIUM (24): **24 confirmed** (3 agents fell over on structured output, checked by hand against the code), 0 rejected. One reclassification: "Top result" artist/album ([shared.ts:821](../src/lib/innertube/shared.ts)) → low.

**Step 6 done (2026-07-03).** LOW (36): triage without agents. 25 confirmed (by reading the code, or verified earlier), 11 got light triage (plausible, not verified deeply, they are low), **0 false**. Marked directly in the "Low" section below.

**Step 8 done (2026-07-03), Track B started.** git init + baseline commit (`main`), fixes on branch `fix/review-p0` (2 commits). 6 high fixed (#3 CREATE_NO_WINDOW, #4 Genius validation, #5 repeat-one, #6 dup videoId, #9 invalidation key, #10 error-card guard on 2 routes) + 1 low (persist whitelist). Both static checks green. The findings are marked 🔧 below. High #11 is partial: git is there, but ESLint/Prettier/tests/CI are not yet (P4). Left: Step 9 (cross-window/cache), Step 10 (medium), Step 11 (low cleanup + the 4 medium candidates below), Step 12 (final).

**Step 9 done (2026-07-03).** Cross-window/cache + security, 2 commits: #8 (floating window without persist, it no longer clobbers the cache or the account), #7 (playNow/playShelfItems/enqueueNext/enqueueEnd forwarded to the main window), SSRF medium #1 (host allowlist + no redirects in cache_cover), #1 (per-launch token as a path prefix on the proxy). `tsc` + `cargo check` green. **⚠️ #1 needs a runtime smoke test**, the streaming routing change was only checked by compiling. Fixed so far: 9 high + the SSRF medium + 1 low. Left: Step 10 (medium), Step 11 (low + the 4 candidates below), Step 12 (final).

**4 candidates to raise to medium** (from the triage):
- [app-shell.tsx:43](../src/components/layout/app-shell.tsx), the global Tab block breaks keyboard navigation (a11y).
- [player-bar.tsx:310](../src/components/layout/player-bar.tsx), wheel-unmute drops the volume to 5% (a setting users lose often).
- [track-list.tsx:415](../src/components/shared/track-list.tsx), 2 menu controllers + 2 query observers + 2 dialogs per row in a virtualized list (perf).
- [shared.ts:119](../src/lib/innertube/shared.ts), stale cookies for 5 min after resetAuthCache (auth correctness, narrow window).

**Steps 10-12 done (2026-07-03), Track B finished.** 9 commits on `fix/review-p0`. Final validation: `pnpm build` (tsc + vite) exit 0, `cargo check` exit 0; youtubei.js confirmed out of the bundle (settings chunk 277KB→12KB, about 265KB saved).

**Fixed across Track B in total: 11 high, ~20 medium, ~18 low.** Every finding is marked 🔧 below with a note on the fix.

**Deferred (deliberately, with reasons: changes without a runtime test are risky, or it is purely organizational refactoring):**
- **medium #7** ([floating-player-sync.tsx:395](../src/components/layout/floating-player-sync.tsx)), "a 300ms pause = drop": without a real end-of-drag signal (mouse button) this can't be fixed properly; #6 closed the neighbouring hidden-window case.
- **medium #9** ([player-more-menu.tsx:194](../src/components/layout/player-more-menu.tsx)), source-switch dedup: "will drift", not a bug; pulling the shared function out of the 2 components is deferred.
- **medium #17** (the big home/explore pagination dedup), the specific bug (readRun/simpleText) is fixed; deduping the ~90 lines is deferred.
- **medium #21** (musixmatch/genius error semantics), lrclib (the main one) is fixed; the other two follow the same pattern and are deferred.
- **medium #24** ([library.tsx:133](../src/routes/library.tsx)), unifying the double liked cache: a risky key refactor.
- **medium #20→low** (top-result artist), needs careful browseEndpoint mapping (risk of breaking navigation).
- **low (perf/refactoring):** track-list per-row observers (#415), grab-bag player-bar (#1), card re-render (#387), formatter dedup (#36), infinite-scroll boilerplate; `noUncheckedIndexedAccess` (a big typing job on the Record<any> layer); per-account buckets (search-history, pinned); query-cache per-account/stringify perf; narrowing the CSP (dev/prod share one config); tooltip-drag (#251), sticky lyrics-source pref (#102).
- **Rust low:** plaintext cookies on non-Windows (#123), the accounts.json RMW race (#212), login-profile leak (#399), by-design cookie exposure (#611). Platform and lifecycle things, deferred.

**High #11 (the safety net) is fully closed (2026-07-03).** git ✅; added ESLint (flat config) + Prettier + vitest (22 tests on the pure logic from the review: parse-lrc/offset, Genius match, track count) + GitHub Actions CI (typecheck+lint+test+build+cargo test). Helpers moved into pure modules (`lyrics/match.ts`, `innertube/parse-count.ts`) so they can be tested. Plus 2 Rust tests, including **`nested_token_prefix_gates_routes`**, which checks the proxy auth token (#1) deterministically and removes the earlier caveat about a runtime smoke test of the routing.

**All gates green on `main`:** `tsc` · `eslint` (0 errors) · `vitest` 22/22 · `vite build` · `cargo test` 2/2. Branch `fix/review-p0` is merged into `main` (14 commits). All that is left is a user smoke test of **audio** playback (checked by ear, the code path and the routing are already under tests).

Refined verdicts and fixes are in the "High"/"Medium"/"Low" sections below. **Verification result: 0 false out of 72 findings.**

**Minimal option** (if the limits are really tight): Steps 1-2 only (verify the 12 high, 🤖12 total across two sessions), and I triage medium/low myself without agents. That gives a solid list of ~10 real priority problems.

---

# 📋 Final report

## Verdict

**ytm-native is a strong, well thought out project, well above the average for hobby code, but it has no safety net at all (git/linter/tests/CI) and three systemic weaknesses that have already started breaking behavior.** The architecture is mature: disciplined layering (raw InnerTube + parsers → Zustand stores → UI), a well designed bridge between the windows, "why" comments everywhere, deliberate race guards. The problems cluster not in "bad code" but in a few root causes, and fixing those is local work that pays back many times over.

Both static checks are green (`tsc --noEmit`, `cargo check`). The codebase is ~17k lines (TS + Rust).

## In numbers

| | Found | Confirmed | False | Reclassified |
|---|---|---|---|---|
| High | 12 | 11 | 0 | 1 → low (dead code) |
| Medium | 24 | 24 | 0 | 1 → low |
| Low | 36 | 25 + 11 (light triage) | 0 | 4 candidates → medium |
| **Total** | **72** | **72** | **0** | - |

Verification was adversarial: every high/medium was checked by a separate agent told to refute it, and some were checked by hand against the code. **Zero false positives.** The check even caught mistakes in the reviewers' own wording (wrong line numbers and keys in high #2, #8, #9), a sign that the filter was working and not rubber-stamping.

## Strengths (do not rewrite these)

- **The file and process boundaries in Rust** are done carefully: yt-dlp via an argument array with no shell (no command injection), video_id and filename sanitization (no path traversal), DPAPI cookie encryption on Windows, magic-byte MIME sniffing, a 32 KB floor against storyboard stubs.
- **Race guards where people usually forget them**: a resolve token during stream resolution, StrictMode-safe teardown of Tauri listeners, a `partialize` that never auto-starts playback on launch, versioned store migrations.
- **InnerTube parsing is defensive**: optional chaining, a tree-walk with a WeakSet guard against cycles, token-echo and a 200-page cap in pagination, excellent comments about the quirks of YT responses.
- **The thumbnail fallback chain** (iTunes → disk cache → largest variant, with onError demotion and cancellation) is genuinely thorough.

## Root causes (fix here and a lot of findings close at once)

1. **State sync between windows** is the biggest architectural weakness. The floating window is a separate JS context, it shares localStorage keys with the main one on a last-writer-wins basis, and the close event doesn't tell user intent apart from a programmatic close. → high #7, #8; medium #5, #6, #7, #22; low layout.ts. **Fix direction:** a floating window with no persist cache; the full set of actions in the bridge; a discriminator for user vs programmatic close.
2. **Query-key drift.** Ad-hoc string keys are scattered across 15+ files and have already diverged (dead invalidations, a dead persist whitelist, duplicate liked caches). → high #9; medium #13, #24; low query-client. **Fix:** centralize the keys in one factory module.
3. **Error semantics collapsed into "no data".** `null` means both "empty" and "failed"; HTTP 200 counts as success; a network failure is cached as "no lyrics" for an hour. → medium #18, #21; low musixmatch, stream.ts:79. And the mirror extreme is high #10, where a continuation error wipes what was already loaded. **Fix:** tell "empty" and "error" apart, check the response status, and don't drop loaded UI because a follow-up fetch failed.
4. **The store→engine contract runs through derived strings.** Track transitions are passed as a videoId string and a boolean `playing` rather than explicit events or tokens; some async paths have no staleness guard. → high #5, #6; medium #14, #15. **Fix:** a monotonic playToken per transition; a guard against stale async results.
5. **yt-dlp lifecycle and downloads-map races in Rust.** No timeout or kill, a write error leaves success=true, check-then-insert is not atomic. → medium #2, #3, #4; low #1365. **Fix:** timeout+kill, `ok=false` on a write error, an atomic get-or-spawn.
6. **The network trust boundary.** A local proxy with no auth (CSRF/DNS rebinding), SSRF in cache_cover. → high #1; medium #1. **Fix:** a Host check + a per-launch token; a host allowlist + blocking private IPs and redirects.
7. **No safety net** (the meta cause). With no git, tests or CI, nothing can catch a regression in any of the above. → high #11.

## Recommended order (roadmap)

- **P0, cheap and worth the most** (half a day): `git init` + a first commit; `CREATE_NO_WINDOW`; repeat-one; the silent dup videoId; guard the playlist error card ([playlist.$id.tsx:143](../src/routes/playlist.$id.tsx)); Genius match validation; the right invalidation key for add-to-playlist.
- **P1, security**: an auth token + Host check on the local proxy; an allowlist + blocking redirects and private IPs in cache_cover.
- **P2, multi-window**: a floating window without persist; finish the actions in the bridge; tell user and programmatic close apart.
- **P3, correctness**: check mutation status; centralize the query keys; error semantics; yt-dlp lifecycle.
- **P4, quality and tooling**: ESLint + Prettier + vitest on the shared.ts parsers; delete the dead youtubei.js (-277 KB of bundle); dedup (pagination, source-switch, formatters); `noUncheckedIndexedAccess`; update the README, clean the junk out of the root.
- **4 low → reconsider as medium**: the Tab block (a11y), wheel-unmute resetting the volume, per-row observers in TrackList, stale cookies after reset.

## Limits of this review (honestly)

Static analysis, without running the app. The bugs are argued from reading the code, not reproduced live. The reviewers were capped at 12 findings per subsystem; two buckets (`innertube`, `architecture`) hit the cap, so there may be an uncovered tail there. 11 of the 36 low got light triage rather than line-by-line verification. All of that is marked in the statuses below.

---

# Appendix: detailed findings

Source: 9 domain reviewers (multi-agent). The static checks `tsc --noEmit` and `cargo check` are both green. Verification of the findings is marked in the status as the batches go through.

Statuses: ⬜ not verified · ✅ confirmed · ❌ rejected (false) · 🔧 fixed

## High (12)

### [1] ✅ No safety net at all: the project is not a git repository and has no linter, formatter, tests, or CI
- **HIGH / quality** — `package.json:6`
- **Verdict:** CONFIRMED / high. Checked directly: `git rev-parse` → not a git repository; no ESLint/Prettier/vitest/jest configs; not a single *.test/*.spec; scripts = dev,build,preview,tauri; no .github/workflows.
- **Fix (refined):** git init + a first commit; add ESLint (typescript-eslint) + Prettier + a lint script; vitest with fixture-based tests for the shared.ts parsers.
- A ~17k-LOC TypeScript + Rust codebase lives outside version control: any bad refactor, disk failure, or accidental `rm` is unrecoverable, and there is no way to bisect regressions in a codebase that talks to an unstable reverse-engineered API (InnerTube payload shapes change under you; without history you cannot tell when a parser broke). package.json has no lint/format/test scripts, no ESLint or Prettier config exists anywhere, and there are zero tests even for the pure parser functions in src/lib/innertube/shared.ts (mapResponsiveListItem, parseDuration, looksLikeDate) which are the most regression-prone and most testable code in the repo. The `.gitignore` is carefully maintained for a repo that doesn't exist.
- **Fix:** git init + initial commit immediately; add ESLint (typescript-eslint) + Prettier + a `lint` script; start a vitest suite with fixture-based tests for the shared.ts parsers.

### [2] ⚠️ resolve_stream_ytdlp is a sync Tauri command that runs a blocking yt-dlp subprocess on the main thread, freezing the entire UI
- **HIGH / perf** — `src-tauri/src/lib.rs:1015`
- **Verdict:** PLAUSIBLE / high (severity → **low**) — The technical mechanism is correct: resolve_stream_ytdlp (src-tauri/src/lib.rs:1015) is a NON-async `#[tauri::command]` (no `async` keyword, no `(async)` attr) that calls the blocking `std::process::Command::new("yt-dlp").output()` (lines 1020-1032). It is registered in generate_handler! (line 1777). Per Tauri docs, sync commands run on the main thread (confirmed for tauri 2.10.3 in Cargo.lock), so IF invoked it would block the event loop for the 2-10s of yt-dlp network I/O and freeze windows. BUT the finding's central reachability claim — "Invoked live from src/lib/innertube/player.ts:75" during playback — is false. The JS wrapper resolveStream() (src/lib/innertube/player.ts:65, the only c…
- **Fix (refined):** src-tauri/src/lib.rs:1015 — make the command non-blocking so it stays safe if a caller is ever wired up: change `fn resolve_stream_ytdlp(video_id: String)` to `async fn resolve_stream_ytdlp(video_id: String)` and wrap the blocking subprocess in `tokio::task::spawn_blocking(move || { std::process::Command::new("yt-dlp")...output() }).await.map_err(|e| e.to_string())?`, or switch to `TokioCommand` + `.output().await` like spawn_downloader at line 1265. Alternatively, if resolveStream()/resolve_stream_ytdlp are confirmed unused, delete src/lib/innertube/player.ts:65's resolveStream and remove resolve_stream_ytdlp from the handler (line 1777) and its definition.
- In Tauri 2, non-async #[tauri::command] fns execute on the main (event-loop) thread. resolve_stream_ytdlp calls std::process::Command::output() on yt-dlp, which performs multiple network round-trips to YouTube and typically takes 2-10+ seconds (much longer on throttled connections). It is live code: src/lib/innertube/player.ts:75 invokes it during stream resolution. While it runs, the event loop is blocked — the window cannot repaint, move, or respond to clicks, and all other windows (floating player, login) freeze too.
- **Fix:** Make it `async fn` and use tokio::process::Command (or keep sync but annotate #[tauri::command(async)] and wrap in spawn_blocking).

### [3] 🔧 yt-dlp is spawned without CREATE_NO_WINDOW, so release builds pop up a visible console window for every download/resolve
- **HIGH / bug** — `src-tauri/src/lib.rs:1279`
- **🔧 Fixed (branch fix/review-p0):** CREATE_NO_WINDOW on both yt-dlp spawns (#[cfg(windows)]).
- **Verdict:** CONFIRMED / high — Verified end-to-end. main.rs:2 sets `#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]` (comment: "Prevents additional console window on Windows in release"), so release builds run console-less. Both spawn sites omit CREATE_NO_WINDOW: lib.rs:1020 `std::process::Command::new("yt-dlp")` in resolve_stream_ytdlp uses only .args().output() with no .creation_flags; lib.rs:1265 `TokioCommand::new("yt-dlp")` in spawn_downloader chains .stdout/.stderr/.spawn() with no .creation_flags (call spans lines 1265-1282). Imports at lib.rs:1-11 do not include std::os::windows::process::CommandExt, and a grep for creation_flags/CREATE_NO_WINDOW/CommandExt across all of src-tauri/src returned …
- **Fix (refined):** Add `use std::os::windows::process::CommandExt;` and set the CREATE_NO_WINDOW flag on both spawns. At lib.rs:1020, insert `.creation_flags(0x08000000)` into the std::process::Command chain (e.g. right after `std::process::Command::new("yt-dlp")`). At lib.rs:1265, add `cmd.creation_flags(0x08000000);` after `let mut cmd = TokioCommand::new("yt-dlp");` (tokio::process::Command also exposes creation_flags on Windows). Gate the import/calls behind `#[cfg(windows)]` (or a small `set_no_window` helper) so non-Windows builds still compile.
- main.rs sets windows_subsystem = "windows" for release, so the app has no console. When a console-less GUI process spawns a console-subsystem child (yt-dlp.exe) with default creation flags, Windows allocates and shows a new console window for the child. Both spawn sites — std::process::Command in resolve_stream_ytdlp (line 1020) and tokio::process::Command in spawn_downloader (line 1265) — omit creation_flags. Result: in a release build, every track play/prefetch flashes (and holds, for the duration of the download) a black console window that steals focus.
- **Fix:** On Windows add `use std::os::windows::process::CommandExt; cmd.creation_flags(0x08000000 /* CREATE_NO_WINDOW */);` to both spawn sites (tokio's Command exposes the same method).

### [4] 🔧 Local stream server has permissive CORS and no Origin/Host/token validation on side-effecting endpoints (CSRF / DNS-rebinding).
- **HIGH / security** — `src-tauri/src/lib.rs:1667`
- **🔧 Fixed (branch fix/review-p0):** The routes are nested under a per-launch token `/{token}/…`; the token is baked transparently into the base URL and the cover URL. A web page that only knows the port cannot build a valid URL. ⚠️ Needs a runtime smoke test (play a track, load a cover).
- **Verdict:** CONFIRMED / high — The stream server binds 127.0.0.1:0 (src-tauri/src/lib.rs:1669) and its ONLY middleware is CorsLayer::permissive() (lib.rs:1667). No Origin/Host/Referer/token/CSRF check exists anywhere in the router or handlers — a full grep for Origin/Host/token/middleware/from_fn over lib.rs returns only unrelated login-window/cookie code (lines 426/585/601), nothing on the /stream|/prefetch|/cover routes (routes at lib.rs:1662-1666). Side-effecting endpoints confirmed: prefetch_handler (lib.rs:1579) calls spawn_downloader (lib.rs:1616→1250) which runs `yt-dlp` (lib.rs:1265) and writes into cache_dir/ephemeral_dir; stream_handler (lib.rs:1394) does the same and additionally returns the downloaded audio b…
- **Fix (refined):** Add a Host-header allowlist + shared-secret token gate to the stream server. Concretely, in src-tauri/src/lib.rs at the Router (lib.rs:1662-1667): (a) add a tower middleware (axum::middleware::from_fn) that rejects (403) any request whose Host header is not exactly `127.0.0.1:{port}` or `localhost:{port}` — this defeats DNS-rebinding since rebinding requests carry the attacker's hostname; and (b) generate a random per-launch token (e.g. 128-bit hex) stored in StreamServer, require it on /stream and /prefetch as a header (e.g. `X-Stream-Token`) or path/query segment, and have the frontend include it in the URLs built at lib.rs:1171/1238 (extend those helpers to append the token). Reject miss…
- The axum server binds to 127.0.0.1 (good) but layers CorsLayer::permissive() and performs zero request-origin validation: no Origin check, no Host-header check, no auth token/secret. /prefetch/:video_id (line 1579) and /stream/:video_id (line 1394) both have side effects — they spawn a yt-dlp child process and write files into the persistent cache_dir, which is never size-bounded. Attack: a user visits any malicious web page or malicious ad iframe in their normal browser. Its JS port-scans loopback and fires simple GETs: for(id of manyIds) fetch(`http://127.0.0.1:${p}/prefetch/${id}`,{mode:'no-cors'}). A no-cors GET is not blocked by CORS and needs no preflight, so once the correct random port is hit the app spawns unbounded yt-dlp processes and fills the disk (CPU/disk DoS). Via DNS rebinding (evil.com rebound to 127.0.0.1) the permissive CORS additionally lets the page READ /stream responses, turning the victim's machine into an anonymizing YouTube-download proxy and allowing probing of cached content. The only mitigant is the random ephemeral port, which port-scanning/rebinding defeats.
- **Fix:** Reject requests whose Origin/Host is not the app's own webview (validate Host == 127.0.0.1:port and drop/deny cross-origin Origins), replace CorsLayer::permissive() with an explicit allowlist (or none, since the webview is same-origin to the tauri asset scheme, not http), and add a per-launch random bearer token the frontend obtains via get_stream_base_url and must present on every request.
- _Related:_ Local proxy uses CorsLayer::permissive with no auth token: any website in the user's browser can drive downloads and probe listening history

### [5] 🔧 Floating player window persists its own independent query cache to the same localStorage key as the main window, clobbering it and resurrecting the previous account's data after a switch
- **HIGH / bug** — `src/components/layout/floating-player-app.tsx:54`
- **🔧 Fixed (branch fix/review-p0):** The floating window now uses a plain QueryClientProvider with no persist, so it no longer clobbers the shared query cache or resurrects the previous account's data.
- **Verdict:** CONFIRMED / high — End-to-end confirmed. (1) Shared persister key: src/lib/query-client.ts:22-26 defines a single `persister` singleton writing localStorage key `ytm-native-query-cache`. It is imported and mounted by BOTH the main window (via App.tsx -> RouterProvider -> __root.tsx AppShell) and the floating window (src/components/layout/floating-player-app.tsx:22-28, 54-66 mounts PersistQueryClientProvider with the same `persister` and the same `queryClient` singleton). (2) Same origin -> shared localStorage: src-tauri/src/lib.rs:682-685 builds the floating window with WebviewUrl::App("index.html?floating-player=1") and no `data_directory` override (contrast the login window at lib.rs:413-418 which DOES set …
- **Fix (refined):** Give the floating window a non-persisting client OR a distinct/disabled persister. Minimal: in src/components/layout/floating-player-app.tsx:54-66, replace `PersistQueryClientProvider` with a plain `QueryClientProvider` from @tanstack/react-query (the floater has no cold-start hydration requirement that justifies persistence — it only mirrors live playback state via Tauri events). This stops the floater from ever writing to `ytm-native-query-cache`, so it can no longer clobber/resurrect the main window's account snapshot. If cold-start cached lyrics/liked in the floater are desired, instead pass a separate persister created with a different key (e.g. key: "ytm-native-query-cache-floating") …
- Both windows are separate webview JS contexts with separate `queryClient` instances, but both mount PersistQueryClientProvider with the shared `persister` (key `ytm-native-query-cache`, src/lib/query-client.ts:22). On mount the floating window restores the persisted snapshot into ITS cache, then persists its whole cache on every cache event (it does run queries: `["lyrics", ...]` via useLyricsSources — a persistable key per shouldPersistQuery — and `["liked", videoId]` via LikeDislikeButtons in PlayerBar). Failure scenario 1 (cross-account leak): with the floating window open, the user switches accounts; `useAccountsChangedListener` (src/lib/store/accounts.ts:129) runs `qc.clear()` only in the main window and the persister writes an empty cache — but the floating window never receives `accounts-changed`, still holds account A's restored home/library/liked-songs snapshot, and its next lyrics/liked fetch on a track change re-persists account A's data to disk. Next cold start hydrates account A's home feed and liked-songs hearts while account B is active. Failure scenario 2 (staleness): even on one account, the floating window's stale restored snapshot overwrites fresher data the main window persisted seconds earlier — last writer wins.
- **Fix:** Don't mount PersistQueryClientProvider in the floating window (use a plain QueryClientProvider) — it has no cold-start rendering need that justifies persistence; or give it a separate persister key with persistence disabled for writes.

### [6] 🔧 Add-to-playlist invalidates a query key no query ever uses, so the playlist page never refreshes
- **HIGH / bug** — `src/components/shared/track-context-menu.tsx:171`
- **🔧 Fixed (branch fix/review-p0):** Invalidate the real key ["playlist-pages"].
- **Verdict:** CONFIRMED / high — The core defect is real. runAddToPlaylist invalidates ["playlist", p.id] (track-context-menu.tsx:171) but the ONLY playlist-content query in the app keys on ["playlist-pages", id] (playlist.$id.tsx:54). TanStack Query matches from the head; head "playlist" never prefix-matches a query whose head is "playlist-pages", so the invalidation is a guaranteed no-op and the open playlist page shows stale contents after adding a track. No other invalidation covers the playlist page — the only nearby invalidations are in NewPlaylistDialog.submit (user-playlists, library), not the playlist-pages query. The persist half is also confirmed: shouldPersistQuery whitelists head==="playlist" (query-client.ts:…
- **Fix (refined):** track-context-menu.tsx:171 — change `await qc.invalidateQueries({ queryKey: ["playlist", p.id] });` to invalidate the actual key with prefix matching, which is id-shape-agnostic: `await qc.invalidateQueries({ queryKey: ["playlist-pages"] });` (invalidates every open playlist page; the router refetches only the active one). Additionally, query-client.ts:37 — change `head === "playlist" ||` to `head === "playlist-pages" ||` (mind the 500 KB per-query persist budget for long playlists) or remove the dead entry.
- runAddToPlaylist calls qc.invalidateQueries({ queryKey: ["playlist", p.id] }), but the playlist page uses ["playlist-pages", id] (src/routes/playlist.$id.tsx:54) and its route id is the VL-prefixed browseId ("VLPL…" from shelf cards) while p.id is the raw "PL…" id — mismatch on both the head and the id shape. Scenario: user opens a playlist, goes back, right-clicks a track → Add to playlist → picks that playlist, navigates back within staleTime → the added track is missing and no refetch fires; the invalidation is a 100% no-op. The same phantom key family exists in query-client.ts:37 where shouldPersistQuery allowlists head === "playlist", so playlist pages are also silently never persisted.
- **Fix:** Invalidate ["playlist-pages"] (prefix match) or normalize ids (VL-prefix) and target ["playlist-pages", `VL${p.id}`]; update the persist allowlist to "playlist-pages".
- _Related:_ runAddToPlaylist invalidates query key ["playlist", id] which no query uses, so the playlist page shows stale contents; Query keys are ad-hoc string literals scattered across 15+ files and have already drifted: invalidations and the persist whitelist target keys that no query uses

### [7] 🔧 Track-change effect keys on videoId string, so advancing to a queue entry with the same videoId stalls playback in permanent 'loading'
- **HIGH / bug** — `src/lib/audio-engine.ts:183`
- **🔧 Fixed (branch fix/review-p0):** Added the queue index to the deps of the resolve effect.
- **Verdict:** CONFIRMED / high — The stream-resolution effect at src/lib/audio-engine.ts:132-183 has dep array [streamVideoId, videoId] (line 183). videoId is s.queue[s.index]?.videoId (line 119) and streamVideoId is resolveStreamId(videoId, byVideoId) (lines 128-130), which per src/lib/store/track-source.ts:132-140 is a pure function of videoId + source prefs. React skips the effect when both strings are Object.is-equal. In src/lib/store/playback.ts, next() (lines 298-308) and goTo() (lines 338-346) both set {index, status:"loading", streamUrl:undefined, playing:true} — index changes but if the target queue entry has the same videoId as the current one, the videoId/streamVideoId strings do NOT change, so the effect is ski…
- **Fix (refined):** src/lib/audio-engine.ts: make the stream-resolution effect re-run on track-selection identity, not just the videoId string. Minimal change: extend the selector at lines 117-122 to also return the current index (e.g. add `index: s.index` to the useShallow object) and add `index` to the effect dep array at line 183 → `[streamVideoId, videoId, index]`. This forces re-resolution + play() when next()/goTo()/prev() advance to a different queue slot holding the same videoId. Alternatively add a monotonic playToken to the store bumped in next/goTo/prev and depend on it.
- The effect deps are [streamVideoId, videoId] — both strings derived from the current track. If the queue contains the same videoId twice in a row (a playlist with a duplicate, radio-appended dupes, or goTo/History-tab navigation to another entry of the currently playing song), next()/goTo() updates index and sets status: "loading", streamUrl: undefined, playing: true — but neither string changes, so React skips the effect: no re-resolve, no el.play(). Coming from 'ended' the element is paused at the end of the file, so the app is stuck with a spinner (player bars show a loader when status==="loading" && playing), playing: true, and total silence until the user manually skips again.
- **Fix:** Include the queue index (or a per-transition nonce/playRequestId set by next/prev/goTo/playNow) in the effect dependencies instead of relying on videoId identity.

### [8] ✅ youtubei.js is a dead dependency whose parser still ships (~270 KB) and which maintains a second, drift-prone cookie cache
- **HIGH / quality** — `src/lib/innertube/client.ts:1`
- **Verdict:** CONFIRMED / high. getInnertube/getMusic have no external callers (only self-references); every external import of client.ts pulls just resetInnertube; client.ts:1 imports youtubei.js at the top level → the chunk dist/assets/settings-*.js is 277 KB with MusicResponsiveListItem/YTNodes markers.
- **Fix (refined):** Delete the youtubei.js client from client.ts, keep a thin resetInnertube→resetAuthCache; move youtubei.js to devDependencies (for scripts/test-player.mjs) or drop it entirely.
- `getInnertube()` and `getMusic()` have zero callers — every import of client.ts across the codebase (settings.tsx, app-sidebar.tsx, accounts.ts) pulls only `resetInnertube`. The real API layer is the hand-rolled one in shared.ts. But because client.ts top-level imports `Innertube, UniversalCache, ClientType` from youtubei.js, the library's parser (with class-registration side effects) lands in the built output: dist/assets/settings-BIrAxFov.js is 277 KB and contains youtubei.js markers (YTNodes, MusicResponsiveListItem, Parser) that never execute. The module also keeps its own cookie cache (`cachedCookieHeader`, loaded once, no TTL) parallel to shared.ts's 5-minute-TTL cache — two auth caches with different invalidation semantics that must be reset in lockstep via resetInnertube→resetAuthCache, a classic drift trap. The `^17.0.1` caret range compounds the risk: youtubei.js breaks between minors as YouTube changes, so a fresh install can pull an incompatible version of a library that isn't even used.
- **Fix:** Delete the youtubei.js client from client.ts (keep only a thin resetInnertube that calls resetAuthCache in shared.ts); move youtubei.js to devDependencies for scripts/test-player.mjs or drop it entirely.

### [9] 🔧 Genius takes the first fuzzy search hit with zero title/artist validation, so it routinely returns lyrics for a completely different song
- **HIGH / bug** — `src/lib/lyrics/genius.ts:83`
- **🔧 Fixed (branch fix/review-p0):** Validate the hit's title/artist (normalizeForMatch + tokenOverlap) before scraping.
- **Verdict:** CONFIRMED / high — genius.ts:60-87 `findSongUrl` builds query `q = "${p.title} ${p.artist}"` (line 61), filters hits only on `h.type === "song"`, `h.result?.url` present, and `lyrics_state !== "unreleased"` (lines 77-82), then returns `usable[0]?.result?.url` (line 83). It never compares `result.title` or `result.primary_artist.name` (both declared in the response type at lines 38-39) against `p.title`/`p.artist`, so the first Genius-ranked hit is taken regardless of whether it is the same song. `fetchGeniusLyrics` (lines 46-58) adds no guard — it just scrapes whatever URL is returned. In sources.ts, all three sources query in parallel (lines 30-73) and `best` selection (lines 81-95) picks the first source wi…
- **Fix (refined):** In src/lib/lyrics/genius.ts findSongUrl (around lines 77-83), before returning add a title/artist match check. Normalize (lowercase, strip punctuation/feat./parenthetical) both sides and require the chosen hit's `result.title` to contain/closely match `p.title` and, when `p.artist` is provided, `result.primary_artist.name` to match `p.artist`; iterate `usable` and return the first hit passing the check, else return null. Concretely: replace `return usable[0]?.result?.url ?? null;` with a loop that computes normalized title/artist similarity for each `usable` hit and returns the first URL whose title (and artist when known) matches above a threshold, returning null when none qualify.
- findSongUrl queries genius.com/api/search with "title artist" and returns usable[0].result.url without ever comparing the hit's title/primary_artist to the requested track. Genius search is fuzzy and almost never returns zero hits, so for any track Genius doesn't actually have (obscure, regional, non-English — the codebase even notes Russian-artist catalogs in alternate-source.ts), the top hit is an unrelated popular song whose lyrics get scraped and displayed labeled "Genius". Worse, auto-pick in sources.ts only falls through to Genius when LRCLIB and Musixmatch both found nothing — which correlates exactly with the tracks Genius also lacks — so the wrong-song case is the common Genius-auto-selected case. Both syncedlyrics and lyricsgenius (cited in the file header as the model) do a fuzzy title/artist similarity check for exactly this reason.
- **Fix:** After filtering hits, verify hit.result.title and primary_artist.name against the requested title/artist with a normalized similarity check (lowercase, strip punctuation/feat., token overlap or Levenshtein ratio threshold); return null when no hit passes.

### [10] 🔧 Repeat-one never restarts the track: after 'ended' the element stays paused because playing never transitions and no play() is issued
- **HIGH / bug** — `src/lib/store/playback.ts:284`
- **🔧 Fixed (branch fix/review-p0):** Resume inside the audio-engine pendingSeek effect when playing && el.paused.
- **Verdict:** CONFIRMED / high — Traced end-to-end. audio-engine.ts:58-60 onEnded calls store().next(). playback.ts:283-286 repeat==="one" branch runs set({ position: 0, pendingSeek: 0, playing: true }). During normal playback `playing` is already true, so that field does not change value and the [playing] effect (audio-engine.ts:187-199, dep array [playing] at line 199) does not re-run — React bails on an unchanged primitive dep, so el.play() at line 192 is never called. The [streamVideoId, videoId] effect (audio-engine.ts:132-183) also does not re-run because repeat-one leaves queue/index/videoId unchanged, so its el.play() at line 163 never fires. The only other reactor, the pendingSeek effect (audio-engine.ts:218-227),…
- **Fix (refined):** Make the seek reactor resume playback. In audio-engine.ts pendingSeek effect (lines 218-227), after setting el.currentTime = pendingSeek and clearing pendingSeek, add: if (usePlaybackStore.getState().playing && el.paused && el.src) void el.play().catch((e) => { if (e?.name !== "AbortError") usePlaybackStore.getState().setStatus("error", e?.message ?? String(e)); }); This restarts the paused, just-ended element on the repeat-one seek without touching the store contract. Alternatively, handle repeat-one directly in onEnded (audio-engine.ts:58-60): if store().repeat === "one" then { el.currentTime = 0; void el.play().catch(...); } else store().next(); — but the pendingSeek-effect fix also cove…
- With repeat === "one", when a track ends the audio element fires 'ended' -> AudioEngine calls store.next() -> the repeat-one branch sets { position: 0, pendingSeek: 0, playing: true }. But `playing` was already true (nothing set it false on 'ended'), so zustand emits no change for it and the engine's [playing] effect (audio-engine.ts:187) never re-runs. The pendingSeek effect only sets el.currentTime = 0 — per the HTML spec an ended element has paused === true and seeking does not resume playback. Result: every track in repeat-one mode plays exactly once, then playback silently stops while the UI still shows the Pause icon (playing: true, position pinned at 0). The same branch also breaks the engine's error auto-advance: onError calls s.next(), which under repeat-one just seeks the dead element instead of retrying/advancing, leaving playing: true forever.
- **Fix:** In the repeat-one branch, force a real restart: either bump a monotonically increasing playRequestId the engine watches, or set playing false then true, or have the engine's pendingSeek effect call el.play() when store.playing && el.paused.

### [11] 🔧 Floating-player more-menu actions (Play, Play next, Add to queue, Start radio) mutate the floater's mirror store instead of dispatching to the main window
- **HIGH / bug** — `src/lib/store/playback.ts:452`
- **🔧 Fixed (branch fix/review-p0):** Added playNow/playShelfItems/enqueueNext/enqueueEnd to the bridge + the matching cases in the floating-player-sync dispatcher.
- **Verdict:** CONFIRMED / high — End-to-end verified. (1) The floater bridge initFloatingPlaybackBridge overrides toggle/next/prev/seek/setVolume/toggleMute/setShuffle/cycleRepeat/goTo/removeAt/moveTrack/clearQueue/appendToQueue/setAutoRadio (src/lib/store/playback.ts:452-478) but NOT playNow/enqueueNext/enqueueEnd/playShelfItems. (2) Those four store methods (playback.ts:125-197) contain no isFloatingPlayerWindow guard — they call set(...) directly, so in the floater they mutate only the local mirror (no audio engine runs there; floating-player-app.tsx:45-81 renders only PlayerBar). (3) The floater UI reaches them: player-bar.tsx:647 renders <PlayerMoreMenu> unconditionally in the floating variant → player-more-menu.tsx:1…
- **Fix (refined):** In src/lib/store/playback.ts initFloatingPlaybackBridge (the setState object at lines 452-478), add four more overrides that emit actions, and add the matching wiring on the receiver side. Store side: playNow: (track, extras) => sendAction({ type: "playNow", track, extras }), enqueueNext: (track) => sendAction({ type: "enqueueNext", track }), enqueueEnd: (track) => sendAction({ type: "enqueueEnd", track }), playShelfItems: (items, startIndex) => sendAction({ type: "playShelfItems", items, startIndex }). Then in src/components/layout/floating-player-sync.tsx extend the PlaybackAction union (lines 101-115) with those four variants and add corresponding cases to the inbound switch (lines 180-2…
- initFloatingPlaybackBridge overrides only toggle/next/prev/seek/setVolume/toggleMute/setShuffle/cycleRepeat/goTo/removeAt/moveTrack/clearQueue/appendToQueue/setAutoRadio. It does NOT override playNow, enqueueNext, enqueueEnd, or playShelfItems. The floating window's PlayerMoreMenu (src/components/layout/player-more-menu.tsx:160) renders TrackMenuItems (src/components/shared/track-context-menu.tsx:236-266) whose 'Play' calls playNow/playShelfItems, 'Play next' calls enqueueNext, 'Add to queue' calls enqueueEnd, and 'Start radio' calls playShelfItems — all via usePlaybackStore.getState(). In the floating window these mutate the local mirror store only: nothing plays (no audio engine there), the main window's queue is untouched, and the floater's queue/status silently diverge from the authoritative state until the next playback:queue broadcast from main overwrites them. Concretely: in floating layout, open the floater's ⋮ menu and click 'Start radio' — status flips to 'loading' with a new local queue, audio keeps playing the old track, and the fake queue vanishes on the next queue broadcast.
- **Fix:** Override playNow, enqueueNext, enqueueEnd, and playShelfItems in initFloatingPlaybackBridge to emit playback:action events (add corresponding cases to the PlaybackAction union and the dispatcher in floating-player-sync.tsx).

### [12] 🔧 A failed continuation fetch replaces the fully-loaded playlist view with a full-page error card
- **HIGH / bug** — `src/routes/playlist.$id.tsx:143`
- **🔧 Fixed (branch fix/review-p0):** Guard `query.error && !header` (and `&& tracks.length===0` in library.tsx), so a failed continuation no longer wipes the view.
- **Verdict:** CONFIRMED / high — Confirmed end-to-end. src/routes/playlist.$id.tsx:143 does `if (query.error) return <full-page error card>` BEFORE `if (!header) return <PlaylistSkeleton/>` (line 155) and before the main render, so any truthy query.error wipes header + all loaded tracks. src/routes/library.tsx:164 has the same pattern: `if (query.error) return <ErrorCard.../>;` returns before the loaded TrackList (line 182). Both are useInfiniteQuery (playlist.$id.tsx:53, library.tsx:132) on @tanstack/react-query ^5.99.2 (package.json:14). In v5, a failed fetchNextPage() sets the shared query status to 'error' and populates the top-level `error` while `data` keeps prior pages — that is exactly why v5 added isFetchNextPageE…
- **Fix (refined):** Guard the error branches so a continuation failure never blanks an already-populated view. In src/routes/playlist.$id.tsx:143 change `if (query.error)` to only take over when there is no data yet, e.g. `if (query.error && !header)`. In src/routes/library.tsx:164 change `if (query.error)` to `if (query.error && tracks.length === 0)`. When data is already present, render the tracks as normal and instead surface the continuation failure inline near the load-more sentinel (e.g. a small "Couldn't load more — retry" row using query.isFetchNextPageError). This keeps first-load failures showing the full error card while preventing a transient continuation error from wiping the header and loaded tra…
- For TanStack Query v5 infinite queries, a failed fetchNextPage() sets query.error while data still holds all previously loaded pages. PlaylistPageView checks `if (query.error)` before rendering anything, so one transient network failure on any continuation request (retry: 1 globally) wipes the header and every loaded track from the screen. Exposure is multiplied by the eager-drain effect (line 129): picking any sort mode or typing one character in the in-playlist search fires potentially 100+ sequential continuation requests, and a single failure nukes the view mid-drain. Same pattern in src/routes/library.tsx:164 (LikedSongsView returns ErrorCard on query.error even when tracks are loaded). Compare feed-view.tsx which correctly renders the error banner alongside already-loaded shelves.
- **Fix:** Only early-return on error when no data is loaded (e.g. `if (query.error && !header)`); otherwise render the loaded tracks plus an inline retry banner. Apply the same to LikedSongsView in library.tsx.

## Medium (24)

### [13] 🔧 cache_cover performs an unrestricted server-side fetch of a caller-supplied URL (SSRF).
- **MEDIUM / security** — `src-tauri/src/lib.rs:1148`
- **🔧 Fixed (branch fix/review-p0):** SSRF guard in cache_cover: https + a host allowlist (mzstatic/ytimg/ggpht/googleusercontent) + Policy::none() (no redirects).
- **Verdict:** CONFIRMED / high — Verified end-to-end. cache_cover (src-tauri/src/lib.rs:1123-1172) takes a caller-supplied `url: String` and at lines 1142-1149 builds a reqwest::Client with ONLY a 10s timeout — no `.redirect(...)` policy — then does `.get(&url).send()`, caches the bytes to disk (1163-1168), and returns `http://127.0.0.1:{port}/cover/{filename}` (1171) which the webview reads back as an <img> and can therefore read the fetched bytes. No scheme/host/IP allowlist exists anywhere: a grep for redirect/is_private/169.254/localhost guards in lib.rs finds nothing near this code. reqwest is pinned `0.12` (src-tauri/Cargo.toml:30) whose default RedirectPolicy follows up to 10 redirects, so a 302 to an internal addre…
- **Fix (refined):** In cache_cover (src-tauri/src/lib.rs, ~line 1142) harden the fetch: (1) parse `url` and reject any scheme other than https (or http+https), returning Err early; (2) resolve the host and reject private/loopback/link-local/reserved IPs (127.0.0.0/8, 10/8, 172.16/12, 192.168/16, 169.254/16, ::1, fc00::/7, etc.) — ideally re-check on every redirect hop; (3) set `.redirect(reqwest::redirect::Policy::none())` on the Client::builder (or a custom policy that re-validates each hop) so a redirect can't escape the allowlist. Simplest robust option: restrict host to an allowlist of known art CDNs (e.g. *.mzstatic.com) since that is the only legitimate source, and set Policy::none().
- cache_cover(url: String) fetches whatever URL it is handed with reqwest and no scheme/host/IP allowlist, then caches the bytes and exposes them at http://127.0.0.1:<port>/cover/<hash> which the webview can read back. reqwest follows redirects by default (up to 10), so even a CDN-looking URL can redirect to an internal target. Cover URLs originate from remote metadata (iTunes/mzstatic/Genius search results per the capability allowlist), which an attacker who influences or MITMs that metadata can control. Scenario: a crafted album-art field resolves to http://169.254.169.254/latest/meta-data/ or an internal http://192.168.x.x/admin service; the frontend passes it straight to cache_cover; the Rust process fetches it from the user's network and serves the response body back to JS via /cover, enabling exfiltration of intranet/cloud-metadata content. It is also reachable by any script in the webview (unsafe-headers/InnerTube surface).
- **Fix:** Restrict cache_cover to https:// URLs on an explicit host allowlist (the same image hosts already in the CSP img-src), resolve and reject private/loopback/link-local IPs after DNS resolution, and disable or bound redirect following (redirect::Policy::none or a validating policy).

### [14] 🔧 No timeout or kill on the yt-dlp child: a wedged download permanently poisons that track and leaks the process until app restart
- **MEDIUM / bug** — `src-tauri/src/lib.rs:1298`
- **🔧 Fixed (branch fix/review-p0):** Per-read timeout + kill for a wedged yt-dlp.
- **Verdict:** CONFIRMED / high. spawn_downloader (lib.rs:1258-1369) reads stdout in an unbounded loop (1298-1317) and calls child.wait() (1322) with no timeout or kill; state.complete is only set after the process exits (1358). The 120s deadline in stream_handler (1474-1479) just returns HTTP 504, it does not kill the child or touch complete. When yt-dlp hangs, the DownloadState with complete=false stays in srv.downloads forever (the eviction at 1365 only fires 60s AFTER complete) and the process leaks; later /stream requests wait 120s→504 and /prefetch returns 202 with no effect.
- **Fix (refined):** Wrap the read loop + child.wait() in tokio::time::timeout; on timeout call child.kill().await, drop the map entry and set complete=true. Keep the Child (or a CancellationToken) in DownloadState so a hung process can be killed.
- spawn_downloader reads child stdout in an unbounded loop and only sets state.complete after the child exits. The 120 s deadline in stream_handler (line 1474) times out the HTTP request but does nothing to the downloader: if yt-dlp hangs indefinitely (stalled TCP connection, wedged extractor), the DownloadState stays in srv.downloads with complete=false forever. Every subsequent /stream or /prefetch request for that videoId attaches to the dead entry — streams block 120 s then 504, prefetches return 202 and do nothing — and there is no recovery path short of restarting the app. The hung yt-dlp process also leaks.
- **Fix:** Wrap the read loop in tokio::time::timeout (e.g. 180 s of no progress), child.kill() on expiry, and treat it as a failed download; remove the map entry on failure.

### [15] 🔧 A mid-download write error leaves ok=true, so a truncated .part file >= 32 KB gets renamed to .webm and permanently cached
- **MEDIUM / bug** — `src-tauri/src/lib.rs:1304`
- **🔧 Fixed (branch fix/review-p0):** ok=false on a write error, so a truncated .part is not cached.
- **Verdict:** CONFIRMED / high — Traced end-to-end in src-tauri/src/lib.rs spawn_downloader (lines 1294-1356). `ok` is initialized true at line 1297. The write-error path (lines 1304-1307) logs and sets `file = None` but NEVER sets `ok = false` — `ok = false` is only reached at line 1313, inside the stdout READ error arm, a distinct failure mode. After `file = None`, the loop keeps reading stdout to EOF (line 1300 breaks on Ok(0)), so yt-dlp drains its pipe and exits 0. At line 1323 `success = ok && status.success()` therefore stays true. The `.part` file is frozen at the byte prefix written before the error and is never cleaned up on a write error (the only remove_file calls are 1263 stale-cleanup, 1343 rename-failure, 13…
- **Fix (refined):** In src-tauri/src/lib.rs at the write-error branch (currently lines 1304-1307), set `ok = false;` alongside `file = None;` inside the `if let Err(e) = f.write_all(chunk).await { ... }` block. This makes `success` (line 1323) false on any write failure, so the branch at line 1340 skips the rename and the else branch at line 1355 removes the truncated `.part`. Example: `eprintln!("[stream] write .part: {e}"); file = None; ok = false;`.
- In spawn_downloader's read loop, if f.write_all fails (disk full, AV lock), the code logs, sets file = None, and keeps draining stdout with ok still true. When yt-dlp exits 0, part_size reflects only the bytes written before the error; if that partial prefix is >= MIN_AUDIO_BYTES (32 KB — reached within the first second of any real track), the truncated file is renamed to <videoId>.webm. The corrupt entry persists in the Premium cache across restarts: playback cuts off or throws MEDIA_ERR_DECODE partway through, on every replay, until the user clears the cache.
- **Fix:** Set ok = false (or a separate write_failed flag) when the .part write fails, so the file is deleted instead of renamed.

### [16] 🔧 Check-then-insert race in prefetch_handler can spawn two yt-dlp processes writing the same .part file, caching a corrupted track
- **MEDIUM / bug** — `src-tauri/src/lib.rs:1603`
- **🔧 Fixed (branch fix/review-p0):** Atomic check+insert under a single lock.
- **Verdict:** CONFIRMED / high. prefetch_handler checks contains_key under the lock and RELEASES it at 1607, then takes the lock again to insert at 1612-1615. In that gap a concurrent /stream (whose check+insert is atomic, 1450-1469) or a second /prefetch inserts its own DownloadState; both then call spawn_downloader for the same <id>.part, and the second one's remove_file(1263)+File::create(1295) overlaps the first one's writes → a garbled file >32KB → renamed to .webm and served broken from then on.
- **Fix (refined):** Make check+insert atomic under a single MutexGuard (Entry::Vacant), the way stream_handler already does it (1450-1469). Pull get-or-spawn into a shared helper so both paths use the same atomic logic.
- prefetch_handler checks map.contains_key under one lock acquisition (line 1603), drops the lock, then inserts under a second acquisition (line 1612). A concurrent /stream request (whose check+insert IS atomic, lines 1449-1468) or a second prefetch for the same videoId can insert its own DownloadState in the gap. Both then call spawn_downloader for the same map_key: the second downloader's remove_file + File::create on the shared <id>.part path interleaves with the first's writes, producing a garbled file that passes the 32 KB floor, gets renamed to <id>.webm, and is persistently served — the track fails to decode on every play until the cache entry is manually deleted. The trigger is realistic: the frontend prefetches the next track (audio-engine.ts:337) at the same moment the user skips to it.
- **Fix:** Do the contains_key check and insert under a single lock guard (HashMap::entry), mirroring stream_handler.

### [17] 🔧 Clearing the queue (or removing the last track) while in floating mode silently reverts the layout preference to 'right'
- **MEDIUM / bug** — `src/components/layout/app-shell.tsx:115`
- **🔧 Fixed (branch fix/review-p0):** A flag that suppresses the layout auto-revert on a programmatic close.
- **Verdict:** CONFIRMED / high — Traced the full chain end-to-end. (1) hasTrack = index>=0 && index<queue.length (app-shell.tsx:75). (2) clearQueue sets index:-1 (playback.ts:258-267); removeAt on the last remaining current track sets index:-1 (playback.ts:209-218). So hasTrack flips false. (3) The effect at app-shell.tsx:91-101 re-fires on [mode,hasTrack]; with hasTrack now false it takes the else branch and calls invoke("close_player_window") (line 97) even though mode is still "floating". (4) Rust close_player_window calls w.close() (lib.rs:715-719). (5) w.close() raises WindowEvent::CloseRequested for label "player", and the handler UNCONDITIONALLY emits "player-window-closed" with no discriminator for programmatic vs …
- **Fix (refined):** Distinguish programmatic closes from the user clicking X. Minimal frontend-only fix: in app-shell.tsx add a ref flag set right before the programmatic close and cleared when the event arrives. Concretely, add `const programmaticClose = useRef(false);` in AppShell, in the else branch at app-shell.tsx:96-100 set `programmaticClose.current = true;` before `invoke("close_player_window")`, and in the listener at app-shell.tsx:111-117 change the body to: `if (programmaticClose.current) { programmaticClose.current = false; return; } if (useLayoutStore.getState().mode === "floating") setMode("right");`. Add programmaticClose to the effect deps as needed. (Alternative robust fix: have Rust emit a di…
- When hasTrack flips false, the effect at app-shell.tsx:91-101 invokes close_player_window. Rust's close path fires WindowEvent::CloseRequested for label 'player', which emits player-window-closed (src-tauri/src/lib.rs:1812). The listener at app-shell.tsx:111-120 sees mode === 'floating' and calls setMode('right'). So a programmatic close (queue cleared from the floater's own queue panel, or removeAt on the last track) is indistinguishable from the user clicking the X, and the persisted floating preference is destroyed — directly contradicting the comment at lines 72-75 ('The mode itself stays the same; the player just reappears in the chosen slot once a track is loaded'). Next time the user plays a track, the right card appears instead of the floating window they chose.
- **Fix:** Distinguish user-initiated closes from programmatic ones, e.g. set a 'closing programmatically' flag before invoking close_player_window and ignore the next player-window-closed event, or have Rust include a reason payload in the event.

### [18] 🔧 Dock watcher auto-closes the floating player against a hidden (tray-minimized) main window, leaving no visible player while audio continues
- **MEDIUM / bug** — `src/components/layout/floating-player-sync.tsx:376`
- **🔧 Fixed (branch fix/review-p0):** Do not dock against a window hidden in the tray (isVisible).
- **Verdict:** CONFIRMED / high — Traced the full path in src\components\layout\floating-player-sync.tsx and src-tauri\src\lib.rs. Main-window close is close-to-tray, not destroy: lib.rs:1804-1806 calls window.hide() + api.prevent_close(), so the "main" window keeps its label and its last on-screen geometry. computeVirtualCursor (floating-player-sync.tsx:345-372) resolves Window.getByLabel("main") — non-null for a hidden window — and reads main.outerPosition()/outerSize(); on Windows a hidden (SW_HIDE) window still reports its last placed rect, so `inside` (line 364-365) can compute true when the floater center overlaps that invisible rect. Its only guard is `if (!main) return null` at line 350, which covers a destroyed/abs…
- **Fix (refined):** In src\components\layout\floating-player-sync.tsx, inside computeVirtualCursor, after resolving `main` (right after line 350 `if (!main) return null;`), add a visibility guard so a tray-hidden main window never counts as a dock target: `if (!(await main.isVisible())) return null;`. This also short-circuits the drag:floating-position emit in onMoved (which calls computeVirtualCursor), so the snap overlay isn't driven against a hidden window either. (Requires the window `is_visible` capability to be allowed in src-tauri capabilities, alongside the existing allow-outer-position/allow-outer-size perms.)
- checkDock compares the floater's center against main.outerPosition()/outerSize(), which still report the window's last on-screen rect after the main window has been hidden to the tray (lib.rs hides 'main' on close instead of exiting). Scenario: user closes the main window to the tray while music plays via the floating player, then drags the floater across the invisible rect where the main window used to be — after 300 ms the floater closes, player-window-closed reverts mode to 'right', and the user is left with audio playing and zero visible player UI until they restore from the tray.
- **Fix:** Skip the dock check (and the drag:floating-position emit) when the main window is not visible: `if (!(await main.isVisible())) return null;` in computeVirtualCursor.

### [19] ✅ Pausing 300 ms mid-drag over the main window closes the floating player while the mouse button is still held
- **MEDIUM / bug** — `src/components/layout/floating-player-sync.tsx:395`
- **Verdict:** CONFIRMED / high — Confirmed end-to-end. The floating window is dragged via the OS title bar (data-tauri-drag-region, src/components/layout/floating-player-app.tsx:113), which emits NO JS-observable drag-start/end or mouse-button-state event. FloatingDockWatcher hooks me.onMoved (src/components/layout/floating-player-sync.tsx:383-403): each move clears and re-arms a 300ms setTimeout (lines 394-402). onMoved only fires on actual OS position changes, so a held-but-motionless mouse produces no events. After 300ms of silence the timer fires checkDock() (line 395/400), which — via computeVirtualCursor (lines 345-372) testing the floating center against the main window's outer rect (line 364-365) — calls invoke("cl…
- **Fix (refined):** In src/components/layout/floating-player-sync.tsx, gate checkDock() on a real drag-end rather than mere movement-silence. Minimal option: add a Rust command (e.g. cursor_primary_down using the OS async key/mouse state, e.g. via a mouse-state crate or GetAsyncKeyState on Windows) and, in the timer callback around line 395, skip closing while the primary button is still held: `if (await invoke("cursor_primary_down")) { timer = setTimeout(...same..., 300); return; }` before `await checkDock()`. Alternatively, subscribe to me.onDragDropEvent/window focus-blur or a window "moved-end" style signal and only run checkDock on that explicit signal instead of the 300ms silence.
- FloatingDockWatcher treats '300 ms with no onMoved events' as a drop, but there is no actual drag-end/button-state signal. If the user drags the floating window across the main window and hesitates for 300 ms while still holding the OS title-bar drag (e.g. reading something, or slowing down to pass over it), checkDock fires, sees the center inside the main rect, and closes the window out from under the in-progress drag. The comment acknowledges only 'brief crossings' are protected — any pause converts an unfinished drag into an unwanted dock plus a layout-mode revert.
- **Fix:** Gate the dock on an explicit drag-end signal instead of a timeout — e.g. poll the mouse button state via a Rust command, or require the position to be stable AND check on a subsequent onFocusChanged/onMoved silence much longer than 300 ms.

### [20] 🔧 Lyrics scroll position is never initialized on mount nor reset on track change — active line starts off-screen or view shows stale scroll
- **MEDIUM / bug** — `src/components/layout/lyrics-view.tsx:190`
- **🔧 Fixed (branch fix/review-p0):** Snapshot the active line on mount and on track change.
- **Verdict:** CONFIRMED / high — Confirmed the core defect (never scrolls on mount) via Scenario A; Scenario B's specific mechanism is inaccurate but doesn't change the overall verdict. Scenario A (CONFIRMED end-to-end): In src/components/layout/lyrics-view.tsx, TimedLyrics seeds prevActiveRef with the first-render activeIdx (line 187: `const prevActiveRef = useRef(activeIdx);`), and the scroll effect early-returns when activeIdx === prevActiveRef.current (line 190: `if (activeIdx === prevActiveRef.current) return;`). So the effect NEVER performs a scroll on mount. TimedLyrics mounts fresh each time the lyrics popover opens (src/components/layout/player-bar-bottom.tsx:279-292 renders LyricsBody inside a Radix PopoverConten…
- **Fix (refined):** src/components/layout/lyrics-view.tsx:187 — seed prevActiveRef with a sentinel that no valid activeIdx can equal so the effect fires on first mount. activeIdx ranges from -1 upward (findActiveIdx line 147 defaults to -1), so use `const prevActiveRef = useRef(-2);` (or `useRef<number | null>(null)` and compare accordingly). This makes the line-190 guard `activeIdx === prevActiveRef.current` false on mount, letting the effect (lines 192-230) scroll the current active line into view. To also handle the smooth-vs-instant preference, the mount scroll will still animate via rAF, which is acceptable; no other change needed since the delta<1 guard (line 218) already no-ops when already positioned (…
- prevActiveRef is initialized to the first render's activeIdx and the scroll effect early-returns when activeIdx === prevActiveRef.current, so the effect never fires on mount. Concrete scenario A: song is at 2:30, user toggles the queue overlay closed (AnimatePresence mode="wait" remounts TimedLyrics) or opens the player mid-song — the lyrics render scrolled to the top with the active line far off-screen, and stay there until the NEXT line change triggers a 720 ms animated jump across the whole document. Scenario B: user skips to the next track while TimedLyrics stays mounted — lines swap but container.scrollTop keeps the old track's offset; during the new track's intro (activeIdx = -1 the effect returns before scrolling), the user stares at a random mid-lyrics position, and any in-flight rAF animation keeps scrolling toward a target computed for the old lines. PlainLyrics has the same stale-scroll problem across track changes since the <div> is reused and never reset.
- **Fix:** Initialize prevActiveRef to a sentinel (e.g. -2) so the effect centers on mount, add lines to the effect deps (or key TimedLyrics/PlainLyrics by track videoId) and reset scrollTop + cancel the pending rAF when lines change.

### [21] ✅ The song↔video source-switch flow is duplicated verbatim in two components and will drift
- **MEDIUM / quality** — `src/components/layout/player-more-menu.tsx:194`
- **Verdict:** CONFIRMED / high — SourceMenuItems.switchTo (player-more-menu.tsx:194-221) and SourceToggle.switchTo (player-bar.tsx:103-130) are byte-identical in logic: same `if (busy || target === selected) return;` guard, same `const cachedAlt = target === "video" ? record?.video : record?.song;` cached short-circuit, same artistsLine/query construction, same `await findAlternateVideoId(query, track.videoId, target)`, identical toast strings ("No video version found"/"No song version found" and `Couldn't switch source: ${(e as Error).message}`), identical setAlternate+setSelected sequence, and identical `finally setBusy(null)`. Both read the same store (useTrackSourceStore) and helper (findAlternateVideoId). Grep across …
- **Fix (refined):** Extract the shared switch logic into a hook, e.g. `useSourceSwitch(track)` in src/lib/store/track-source.ts (or a new src/lib/innertube/use-source-switch.ts), returning `{ selected, busy, switchTo }` and containing the busy-guard, cached-alternate check, query build, findAlternateVideoId call, toast strings, and setAlternate/setSelected sequence. Then replace the duplicated body in player-bar.tsx:95-130 (SourceToggle) and player-more-menu.tsx:187-221 (SourceMenuItems) with `const { selected, busy, switchTo } = useSourceSwitch(track);`, leaving each component only its distinct JSX (segmented control vs. dropdown items).
- `SourceMenuItems.switchTo` (player-more-menu.tsx:194-221) is a line-for-line copy of `SourceToggle.switchTo` (src/components/layout/player-bar.tsx:103-130): same busy-guard, same cached-alternate check, same `title + artists` query construction, same findAlternateVideoId call, same toast strings, same setAlternate/setSelected sequence. Any future change (e.g. improving the search query, adding album to it, changing error copy) applied to one surface silently diverges the other — the right-card toggle and the bottom-bar menu would resolve different alternate videos for the same track.
- **Fix:** Extract a `useSourceSwitch(track)` hook returning { selected, busy, switchTo } and consume it from both components.

### [22] 🔧 Queue drag-and-drop lands one row lower than the insertion indicator when dragging downward
- **MEDIUM / bug** — `src/components/layout/queue-panel.tsx:234`
- **🔧 Fixed (branch fix/review-p0):** The correct insert index for a downward drag.
- **Verdict:** CONFIRMED / high — Verified the full chain. queue-panel.tsx:466 renders the drop indicator as a line ABOVE the hovered row (`before:-top-px`, gated by isDropTarget = dragOver === queueIdx at line 226), promising insertion BEFORE that row. queue-panel.tsx:234 calls onMoveTrack(dragFrom, queueIdx) passing the raw hovered absolute index. moveTrack (playback.ts:241-242) does `next.splice(from,1)` then `next.splice(to,0,item)` — splice-out shifts later indices down by one. Trace for [A,B,C,D] dragging B (from=1) onto D (to=3): splice(1,1)→[A,C,D], splice(3,0,B)→[A,C,D,B], so B lands AFTER D, not before it as the indicator promised. Upward drag D (from=3) onto B (to=1): splice(3,1)→[A,B,C], splice(1,0,D)→[A,D,B,C] …
- **Fix (refined):** In src/components/layout/queue-panel.tsx line 234, adjust the target index for downward drags to honor the drop-before-target indicator: replace `onMoveTrack(dragFrom, queueIdx);` with `onMoveTrack(dragFrom, dragFrom < queueIdx ? queueIdx - 1 : queueIdx);`. This makes a downward drag onto row D insert before D (matching the top-line indicator), while leaving upward drags unchanged. No change needed in playback.ts moveTrack since its splice-out-then-insert semantics are then correct for both directions.
- The drop indicator renders as a line ABOVE the hovered row (isDropTarget style 'before:-top-px' at line 466), promising insertion before that row. But onDrop calls onMoveTrack(dragFrom, queueIdx), and the store's moveTrack (playback.ts:235-256) splices the item out first, shifting later indices down by one, then inserts at `to`. For a downward drag on queue [A,B,C,D], dragging B onto D (from=1, to=3) yields [A,C,D,B] — B lands AFTER D, one slot below the indicated position. Upward drags are correct, so the behavior is inconsistent between directions.
- **Fix:** For downward moves pass queueIdx - 1 as the destination (or change the indicator to render below the row for downward drags), e.g. onMoveTrack(dragFrom, dragFrom < queueIdx ? queueIdx - 1 : queueIdx).

### [23] 🔧 Infinite-scroll sentinel retries a failing fetchNextPage in an unbounded loop with no backoff
- **MEDIUM / bug** — `src/components/shared/feed-view.tsx:53`
- **🔧 Fixed (branch fix/review-p0):** An error guard in the sentinel effects (4 routes), so there is no endless retry.
- **Verdict:** CONFIRMED / high — Traced the full state machine end-to-end. All four sites (src/components/shared/feed-view.tsx:53-64, src/routes/index.tsx:33-44, src/routes/playlist.$id.tsx:103-119, src/routes/library.tsx:146-162) run an IntersectionObserver effect that guards ONLY on hasNextPage and isFetchingNextPage — never on error/isFetchNextPageError. In query-core 5.99.2 infiniteQueryObserver.js:48-59, isFetchingNextPage = isFetching && direction==="forward" and hasNextPage = getNextPageParam(options, state.data)!=null. In infiniteQueryBehavior.js:95-109, hasNextPage is computed from the LAST SUCCESSFUL page; a failed fetchNextPage appends no page, so state.data is unchanged and getNextPageParam still returns the sa…
- **Fix (refined):** Add an isFetchNextPageError guard so a failed page fetch stops auto-retrying until the user acts. In each site destructure isFetchNextPageError from the query and add it to the effect's early-return guard and dependency array. E.g. src/components/shared/feed-view.tsx:55 change `if (!node || !hasNextPage || isFetchingNextPage) return;` to `if (!node || !hasNextPage || isFetchingNextPage || isFetchNextPageError) return;` and add isFetchNextPageError to the dep array at line 64. Apply the same to src/routes/index.tsx:35/44, src/routes/playlist.$id.tsx:106/119 (guard `query.isFetchNextPageError` inside the isIntersecting check and dep array), and src/routes/library.tsx:149/162. Optionally pair …
- When a next-page fetch fails (offline, API error), isFetchingNextPage toggles true→false, which re-runs the effect, disconnects and recreates the IntersectionObserver; IntersectionObserver always fires its callback once after observe() with the current state, so with the sentinel still visible it immediately calls fetchNextPage() again. hasNextPage stays true (it comes from the last successful page), so the cycle repeats as fast as requests fail — an endless network retry loop hammering the InnerTube edge while the user sits at the bottom of the feed. The same pattern exists in src/routes/index.tsx:33 (Home), src/routes/playlist.$id.tsx:103, and src/routes/library.tsx:146; the playlist eager-drain effect (playlist.$id.tsx:129) is worse — it re-schedules a fetch every 250 ms forever on persistent failure.
- **Fix:** Gate the observer callback on query error state (e.g. don't auto-fetch when the query is in error; require an explicit Retry click), or add exponential backoff / a max-attempt counter before re-arming the observer.

### [24] 🔧 JumpToCurrentButton captures the active row's DOM node once, but the virtualized TrackList unmounts/remounts rows, leaving the effect tracking a detached element
- **MEDIUM / bug** — `src/components/shared/jump-to-current-button.tsx:52`
- **🔧 Fixed (branch fix/review-p0):** Re-query the row inside update (do not hold a detached node).
- **Verdict:** CONFIRMED / high — The effect at src/components/shared/jump-to-current-button.tsx:48-75 resolves `el` once via querySelector (line 52) and closes over that reference in `update` (line 59-68); its deps are only `[activeVideoId, inList]` (line 75), so it never re-runs on scroll. The scroll listener is attached to the persistent `scroller` (main.app-scroll, line 56/71), so `update` keeps firing and reading `el.getBoundingClientRect()` (line 60) on a node that becomes detached. TrackList (src/components/shared/track-list.tsx) virtualizes with overscan 8 (line 135) and getItemKey `${tracks[i].id}:${i}` (line 137), so a row that leaves and re-enters the window is unmounted then remounted as a NEW element — the capt…
- **Fix (refined):** In src/components/shared/jump-to-current-button.tsx, stop closing over a one-time-resolved node. Re-resolve the row inside `update()` on every call: replace the captured `el` usage (lines 52-68) so that `update` does `const el = document.querySelector<HTMLElement>(\`[data-videoid="${CSS.escape(activeVideoId)}"]\`); if (!el) { setActiveOnScreen(false); return; }` and then computes `elRect = el.getBoundingClientRect()` against the still-once-resolved `scroller`. Keep the outer resolve only to obtain and bind the `scroller` for the listener. querySelector at scroll frequency is cheap here, and this makes the effect see whichever node (real row or hidden sentinel) is currently mounted. Deps `[a…
- The effect runs only when [activeVideoId, inList] change and stores `el` from a one-time querySelector. TrackList (useVirtualizer, overscan 8) removes the row element from the DOM once it leaves the virtual window and creates a new element when it re-enters; getBoundingClientRect() on the detached node returns all zeros. Concrete failure: play a track in a long playlist, scroll it out of view (button correctly appears), then scroll back so the row is visible — the button never hides because the stale node reports elRect.bottom=0 which fails the visibility test, and the arrow direction is permanently 'up' (0 <= scrollerRect.top) even when the row is below the viewport. State persists until the active track changes. The onClick handler happens to work because it re-queries the DOM fresh.
- **Fix:** Re-resolve the element inside update() on every scroll event (querySelector is cheap at scroll frequency here), or observe via a MutationObserver / re-run the effect keyed on the virtualizer's rendered range.

### [25] 🔧 Like/unlike mutations patch only ["liked-songs"], leaving the Library Songs view and the LM playlist page stale
- **MEDIUM / bug** — `src/components/shared/like-buttons.tsx:84`
- **🔧 Fixed (branch fix/review-p0):** Invalidate Library Songs + the LM playlist on like.
- **Verdict:** CONFIRMED / high — Verified end-to-end. Like/unlike handlers patch ONLY the membership cache key ["liked-songs"]: like-buttons.tsx:84 and :90 (onLike), and track-context-menu.tsx:133 (runLike), :149 (runRemoveRating), :160 (runDislike). None of them invalidate the two views that render the actual song list. The Library Songs view (library.tsx:133, LikedSongsView) reads from useInfiniteQuery key ["library","liked-songs-pages"] fed by fetchPlaylistFirstPage("LM"). The Liked Songs playlist page (playlist.$id.tsx:54, id==="LM"/"VLLM" per :66) reads from ["playlist-pages", id]. Both derive their visible rows from query.data.pages.flatMap(...tracks) (library.tsx:143, playlist.$id.tsx:78) — caches that are wholly in…
- **Fix (refined):** In both like/unlike code paths, after patching ["liked-songs"], also invalidate the two list views so the visible rows reconcile. In src/components/shared/like-buttons.tsx onLike (after the setQueryData at ~84 and ~90, e.g. add before the toast in each branch): void qc.invalidateQueries({ queryKey: ["library", "liked-songs-pages"] }); void qc.invalidateQueries({ queryKey: ["playlist-pages", "LM"] }); void qc.invalidateQueries({ queryKey: ["playlist-pages", "VLLM"] }); Apply the identical three invalidations in src/components/shared/track-context-menu.tsx after each of runLike (~140), runRemoveRating (~151), and runDislike (~162). (invalidateQueries only refetches active observers, so these …
- The liked-songs membership cache (["liked-songs"]) is patched optimistically, but the Library → Songs tab uses ["library", "liked-songs-pages"] (src/routes/library.tsx:133) and the Liked Songs playlist page uses ["playlist-pages", "LM"/"VLLM"] — neither is invalidated by runLike/runRemoveRating/runDislike (same gap in track-context-menu.tsx:133-166). Scenario: user is on Library → Songs, likes a track via the player-bar heart — the heart fills but the visible list never gains the track while mounted; unliking from the list's own heart leaves the row present. The views only self-correct on remount after the 5-minute default staleTime.
- **Fix:** Also invalidate ["library", "liked-songs-pages"] and ["playlist-pages"] keys for LM/VLLM in the like/unlike/dislike handlers.

### [26] 🔧 Starting a new track while paused audibly resumes the OLD track for the duration of the stream resolve round-trip
- **MEDIUM / bug** — `src/lib/audio-engine.ts:191`
- **🔧 Fixed (branch fix/review-p0):** Clear the old src, so nothing plays on from the old track when starting while paused.
- **Verdict:** CONFIRMED / high — Confirmed end-to-end. In src/lib/audio-engine.ts the track-change effect is declared at line 132 (deps [streamVideoId, videoId], line 183) and the play/pause effect at line 187 (deps [playing], line 199). React runs effects in declaration order, so on a single commit that changes BOTH the current track and playing (the case here), the track-change effect runs first, then the [playing] effect. Store commit proof: playback.ts goTo (line 334-347), playNow (131-140), setQueue (146-155) all set the new index/track AND playing:true in one set(). Stale-src proof: the track-change effect calls el.pause() unconditionally (line 138) but only clears the source when !streamVideoId (line 139-144); for a…
- **Fix (refined):** src/lib/audio-engine.ts, in the track-change effect right after el.pause() (line 138): clear the stale source for the new-track path too, so the [playing] effect's `if (!el.src) return;` guard short-circuits during the resolve round-trip. Add `el.removeAttribute("src");` immediately after `el.pause();` (before the `if (!streamVideoId)` block). el.src is then re-set to track B at line 159 as today, where the same effect handles play() if playing is true. This closes the window without touching effect ordering. (Equivalent alternative: in the [playing] effect, only call el.play() when the store's streamUrl matches el.currentSrc, i.e. read streamUrl and guard `if (el.src !== usePlaybackStore.g…
- playNow/goTo/setQueue change the current track and set playing: true in the same commit. Effects run in declaration order: the track-change effect (line 132) calls el.pause() and kicks off the async streamUrlFor(); then the [playing] effect runs, sees the stale non-empty el.src (old track's URL, old currentTime) and calls el.play() — un-pausing the old track. Concrete: pause song A mid-way, click song B in a shelf -> you hear a ~50-500 ms blip of song A from its old position before B's src lands. The pause() the code adds specifically to prevent bleed is defeated by the play effect one line group below.
- **Fix:** In the [playing] effect, only call play() when the loaded src matches the current resolution (e.g., compare el.src to store streamUrl, or gate on status !== "loading"); alternatively clear el.src synchronously in the track-change effect before the async resolve.

### [27] 🔧 Stale auto-radio fetch appends the old seed's radio tracks to a queue the user has since replaced
- **MEDIUM / bug** — `src/lib/audio-engine.ts:359`
- **🔧 Fixed (branch fix/review-p0):** A staleness guard for the auto-radio append.
- **Verdict:** CONFIRMED / high — Verified end-to-end. The auto-radio effect at src/lib/audio-engine.ts:352-367 fires fetchRadio(seedVideoId) when the current track is last (qIndex >= qLen-1), then at line 360-362 its .then() unconditionally calls usePlaybackStore.getState().appendToQueue(rest) with NO staleness/token check. Contrast the stream resolver in the same file: lines 155 and 179 both guard with `if (token !== resolveTokenRef.current) return;`. The only guard in the radio effect (line 357, radioFetchedForRef.current === seedVideoId) merely prevents duplicate fetches for the same seed — it does not guard the append against a queue replacement. fetchRadio (src/lib/innertube/radio.ts:12) takes no AbortSignal and the e…
- **Fix (refined):** src/lib/audio-engine.ts:359-363: add a staleness guard inside the .then(). Capture the seed at fetch time and re-check current store state before appending, e.g.: `const seed = seedVideoId; fetchRadio(seed).then((tracks) => { const s = usePlaybackStore.getState(); const cur = s.index >= 0 ? s.queue[s.index]?.videoId : undefined; if (cur !== seed) return; /* queue changed; drop stale radio */ if (s.index < s.queue.length - 1) return; /* no longer last */ const rest = tracks.filter((t) => t.id !== seed); if (rest.length) s.appendToQueue(rest); })`. This mirrors the resolveTokenRef pattern already used by the stream resolver.
- The auto-radio effect fires fetchRadio(seedVideoId) when the current track is last in the queue, and the .then() unconditionally calls appendToQueue(rest). Unlike the stream resolver (which has resolveTokenRef), there is no staleness guard. Concrete: autoRadio on, user reaches the last track of playlist A (fetch fires, takes 1-3 s), user immediately clicks an album B (playNow replaces the queue) -> the resolved radio-of-A tracks get appended to album B's queue, so after B's last song the app plays unrelated songs seeded from playlist A.
- **Fix:** Capture a token (or the queue's seed) before the fetch and verify the current queue/seed still matches in the .then() before calling appendToQueue; also reset radioFetchedForRef when the queue is replaced.

### [28] 🔧 iTunes cover-art localStorage cache grows without bound — expired entries are evicted only if the same track is looked up again, and there is no sweep or cap
- **MEDIUM / perf** — `src/lib/cover-art.ts:57`
- **🔧 Fixed (branch fix/review-p0):** Sweep + a cap of 500 + a retry on quota overflow.
- **Verdict:** CONFIRMED / high — Read src/lib/cover-art.ts end-to-end. writeCache (cover-art.ts:57-66) does localStorage.setItem for one key `ytm-cover-itunes:<artist>|<title>` per unique track — including negative/not-found results with a 7-day TTL (line 59). The ONLY eviction is the single-key localStorage.removeItem inside readCache (cover-art.ts:48), which fires lazily only when that exact expired key is read again; there is no sweep, cap, or bulk cleanup anywhere. Grep over src/**/*.{ts,tsx} for `ytm-cover-itunes`, `Object.keys`, and `localStorage` found no eviction loop for these keys. Reachability confirmed: useITunesCover (player-bar.tsx:58-80) fires the effect on every distinct (artistKey,titleKey) change (deps at…
- **Fix (refined):** In src/lib/cover-art.ts, add a bounded sweep mirroring the pattern already used in src/lib/store/track-source.ts. In writeCache (after the successful setItem around line 61), enforce a cap: enumerate keys with `Object.keys(localStorage)` filtering by CACHE_KEY_PREFIX; if count exceeds a cap (e.g. 500), parse each entry and remove those whose expiresAt < Date.now() first, then if still over cap drop the oldest by expiresAt until under a keep-threshold (e.g. 400). Wrap the enumeration in try/catch so it fails silently like the existing writeCache guard. Alternatively run this sweep once on module load. Minimal version: on each writeCache, if `Object.keys(localStorage).filter(k => k.startsWith…
- writeCache adds one `ytm-cover-itunes:<artist>|<title>` key per unique track shown in the now-playing big cover (player-bar.tsx:68). readCache removes an entry only when that exact key is read after expiry, so entries for tracks never played again persist forever regardless of the 30-day TTL. Over months of listening, thousands of keys accumulate (~150–250 bytes each) in the same 5–10 MB localStorage quota that also holds the persisted react-query cache (up to 500 KB per query), the playback queue, and all zustand stores. Failure scenario: quota exhaustion makes localStorage.setItem throw — writeCache swallows it, but createSyncStoragePersister (no `retry` configured) silently stops persisting the query cache, and zustand persist writes start failing, so all persistence quietly degrades with no user-visible signal.
- **Fix:** On startup (or on write failure), sweep keys with the CACHE_KEY_PREFIX and delete expired entries; optionally cap the count with an LRU-ish trim like track-source.ts does.

### [29] 🔧 ~90 lines of pagination/extraction logic duplicated verbatim between home.ts and explore.ts, and readRun in mutations.ts already drifted from shared readRuns
- **MEDIUM / quality** — `src/lib/innertube/explore.ts:83`
- **🔧 Fixed (branch fix/review-p0):** PARTIAL: readRun now reads simpleText (the drift is gone). The big home/explore dedup is deferred.
- **Verdict:** CONFIRMED / high — test
- **Fix (refined):** test
- extractInitialPage, extractContinuationPage, findContinuationInContents, findContinuationInList and the cursorTag/titleSeen shelf-id logic are copy-pasted identically in home.ts (lines 53-108) and explore.ts (lines 83-140). Any fix for a new YT continuation shape must land twice or the feeds diverge silently. Drift has already happened elsewhere: mutations.ts:126 readRun() re-implements shared.ts readRuns() but omits the simpleText branch, so a playlist whose title arrives as {simpleText} (instead of runs) reads as "" and, failing the accessibilityText fallback, is silently excluded from the "Add to playlist" submenu.
- **Fix:** Move the extraction helpers and shelf-id builder into shared.ts and reuse; delete readRun in favor of the exported readRuns.

### [30] 🔧 Like/dislike and edit_playlist mutations treat any HTTP 200 as success without checking the response status
- **MEDIUM / bug** — `src/lib/innertube/mutations.ts:16`
- **🔧 Fixed (branch fix/review-p0):** addToPlaylist checks the envelope status.
- **Verdict:** CONFIRMED / high — Confirmed end-to-end. innertubePost (src/lib/innertube/shared.ts:179-188) only throws on !res.ok (HTTP status); on any 200 it returns res.json() without inspecting responseContext errors or a body status/STATUS_FAILED field. rate() (src/lib/innertube/mutations.ts:11-27) awaits innertubePost and resolves void; it never reads the body beyond a DEV console.debug. addToPlaylist() (mutations.ts:133-141) likewise awaits and ignores the body. The anonymous path is reachable: authHeaders() returns {} when no cookie is present (shared.ts:148-150) and innertubePost sends the request regardless — so a signed-out user genuinely fires an anonymous like/edit that YouTube answers HTTP 200 but persists not…
- **Fix (refined):** In src/lib/innertube/shared.ts innertubePost (after line 186, `const json = ... as YtNode;`), inspect the parsed body for failure before returning: throw if json.responseContext contains an errors array, or if a status field equals a non-success value. Concretely add: `const status = (json as any)?.status ?? (json as any)?.actions?.[0]?.status; if (typeof status === "string" && status !== "STATUS_SUCCEEDED" && status.includes("FAILED")) throw new Error(\`InnerTube ${endpoint} body status ${status}\`); const errs = (json as any)?.responseContext?.errors ?? (json as any)?.error; if (errs) throw new Error(\`InnerTube ${endpoint} returned error: ${JSON.stringify(errs).slice(0,300)}\`);`. Altern…
- rate() and addToPlaylist() ignore the response body. InnerTube returns HTTP 200 for anonymous like/edit calls that persist nothing (the file's own doc comment says so), and browse/edit_playlist returns 200 with status: "STATUS_FAILED" on rejected edits. Scenario: a signed-out user (or one whose cookies expired mid-session) clicks the heart — likeTrack resolves, the UI toasts "Added to Liked", the ["liked-songs"] cache is patched and the heart fills; on the next real fetch the like silently vanishes. Same for "Added to <playlist>" toasts on failed edits.
- **Fix:** Check resp.status === "STATUS_SUCCEEDED" (edit_playlist) and detect the anonymous case (e.g. missing auth cookie / no feedback actions) and throw so callers roll back and show an error.

### [31] 🔧 Radio and /next-fallback parsing skip playlistPanelVideoWrapperRenderer rows, silently dropping tracks
- **MEDIUM / bug** — `src/lib/innertube/radio.ts:26`
- **🔧 Fixed (branch fix/review-p0):** Unwrap playlistPanelVideoWrapperRenderer (radio + playlist).
- **Verdict:** CONFIRMED / high. fetchRadio (radio.ts:25-30) and the identical loop at playlist.ts:133 only read c.playlistPanelVideoRenderer. Grep over src: playlistPanelVideoWrapperRenderer is handled NOWHERE. YTM wraps rows that have both an audio and an MV version in a wrapper (the real renderer sits under primaryRenderer) → those rows silently drop out and the radio queue is shorter than what was actually returned.
- **Fix (refined):** In both loops: const row = c.playlistPanelVideoRenderer ?? c.playlistPanelVideoWrapperRenderer?.primaryRenderer?.playlistPanelVideoRenderer; move the unwrapping into a shared helper in shared.ts so it is not duplicated.
- fetchRadio (and the identical loop in playlist.ts:131-137) only handles c.playlistPanelVideoRenderer. YTM wraps rows that have a song/video counterpart in playlistPanelVideoWrapperRenderer (with the real renderer under primaryRenderer) — a shape every mature InnerTube client (e.g. ytmusicapi's watch parser) special-cases. Scenario: start radio on a track that has both audio and MV versions — the wrapped row (often the seed/current track or several recommendations) is silently omitted from the returned queue, so the radio queue is shorter than what YTM actually returned with no error.
- **Fix:** Unwrap: const row = c.playlistPanelVideoRenderer ?? c.playlistPanelVideoWrapperRenderer?.primaryRenderer?.playlistPanelVideoRenderer; do the same in playlist.ts's /next fallback.

### [32] ✅ Search "Top result" entity is dropped when it is an artist/album (browse onTap) rather than a song
- **MEDIUM / bug** — `src/lib/innertube/shared.ts:821`
- **Verdict:** CONFIRMED / high (severity → **low**) — mapCardShelfFeatured (src/lib/innertube/shared.ts:820-837) reads only card.onTap?.watchEndpoint?.videoId (line 821) and returns null when it is absent (line 822). For an artist/album top hit the musicCardShelfRenderer.onTap is a browseEndpoint (browseId), not a watchEndpoint, so the featured entity is dropped and mapShelfWrapper (line 883-886) never unshifts it — only the trailing contents rows survive. The codebase already knows how to map a browse featured entity: pageTypeToKind (shared.ts:436-442) and mapTwoRowItem's browse branch (shared.ts:546-556) derive kind/round from browseEndpoint pageType, so the drop is purely because the featured mapper ignores the browse case. HOWEVER the spec…
- **Fix (refined):** In src/lib/innertube/shared.ts mapCardShelfFeatured (around line 820-837), handle the browse onTap case in addition to watch. Replace the early videoId-only guard with: const watchId = card.onTap?.watchEndpoint?.videoId; const browse = card.onTap?.browseEndpoint; then if watchId, build the existing kind:"song" item; else if browse?.browseId, map kind via pageTypeToKind(browse.browseEndpointContextSupportedConfigs?.browseEndpointContextMusicConfig?.pageType ?? "") (default fall-through if null), set id=browse.browseId, round=true when kind==="artist", and return that ShelfItem; else return null. This reuses the same pageType-to-kind logic already used by mapTwoRowItem (shared.ts:546-556).
- mapCardShelfFeatured only reads card.onTap?.watchEndpoint?.videoId and returns null otherwise. For artist/album top hits the musicCardShelfRenderer's onTap is a browseEndpoint, so the featured entity itself never becomes a ShelfItem — the "Top result" shelf renders only its trailing song rows. Scenario: search "eminem" with the default "all" filter → the Eminem artist card (the single most relevant result) is missing from the Top result shelf; the user must scroll to the Artists shelf to find it.
- **Fix:** In mapCardShelfFeatured, also handle onTap.browseEndpoint: map browseId + pageType via pageTypeToKind (like mapTwoRowItem does) and emit an artist/album/playlist item.

### [33] 🔧 All three fetchers swallow every error and return null, so network failures are cached as "No lyrics found" for an hour and retry: 1 is dead code
- **MEDIUM / bug** — `src/lib/lyrics/sources.ts:48`
- **🔧 Fixed (branch fix/review-p0):** PARTIAL: lrclib now rethrows network errors so retry works. Musixmatch/Genius follow the same pattern and are deferred.
- **Verdict:** CONFIRMED / high — Read all three fetchers end-to-end and the consumer. Every network path is wrapped in try/catch returning null: lrclib.ts lrclibGet (catch return null, lines 67-69) and lrclibSearch (92-94) -> fetchLrclibLyrics returns null (line 51); musixmatch.ts fetchToken (109-111), findTrackId (215-217), getSubtitle (241-243), getPlainLyrics (269-271) all catch->null, so a transient throw yields null via tryFetch/fetchMusixmatchLyrics (line 154); genius.ts findSongUrl (84-86) and scrapeLyrics (98-100) catch->null -> fetchGeniusLyrics null. Because no queryFn can reject, the react-query promise always resolves, so `retry: 1` in sources.ts:48/60/72 is unreachable dead code (retry only fires on rejection)…
- **Fix (refined):** Distinguish transient network/parse failures from genuine no-lyrics. In each fetcher's catch blocks (lrclib.ts:67-69,92-94; musixmatch.ts:109-111,215-217,241-243,269-271; genius.ts:84-86,98-100) rethrow real errors instead of returning null, and only return null for definitive negative results (e.g. 404, empty result list). Concretely: change `catch { return null; }` to `catch (e) { throw e; }` on the network catch blocks (keep `if (r.status === 404) return null` and empty-result `return null` paths as legitimate nulls). This lets react-query see a rejection so `retry: 1` fires and staleTime does not cache a transient failure as a valid no-lyrics answer. Optionally in sources.ts:47/59/71 ke…
- fetchLrclibLyrics, fetchMusixmatchLyrics, and fetchGeniusLyrics each wrap every network call in try/catch returning null, so the react-query promise never rejects. Consequences: (1) retry: 1 on all three queries can never fire; (2) a transient failure (Wi-Fi blip at track start, DNS hiccup, Genius 403) resolves the query with data=null which staleTime: ONE_HOUR then treats as a fresh, valid "this track has no lyrics" answer — the panel shows "No lyrics found" for the rest of the track (and for the whole hour if the query stays mounted) with no refetch. "Fetch failed" and "source has no lyrics" are semantically different but collapsed into the same cached value.
- **Fix:** Let the fetchers throw (or return a distinct error sentinel) on network/HTTP failures and return null only for genuine not-found responses; react-query's retry and error state then work as configured.

### [34] 🔧 floatingPinned is silently reverted because the two windows share the `ytm-layout` localStorage key with no cross-window sync, and zustand persist rewrites the whole state on any set
- **MEDIUM / bug** — `src/lib/store/layout.ts:31`
- **🔧 Fixed (branch fix/review-p0):** Cross-window sync of ytm-layout via the storage event + rehydrate.
- **Verdict:** CONFIRMED / high — End-to-end confirmed. (1) src/lib/store/layout.ts:25-34 defines a single zustand v5 persist store keyed "ytm-layout" with NO partialize (so setState serializes the whole {mode, floatingPinned} state on every write) and NO storage-event/cross-tab sync — grep across src/ finds no addEventListener("storage"), no createJSONStorage, no partialize. zustand v5 persist only reads storage once at rehydration (mount) and never listens for cross-window writes. (2) Both windows load the same origin (src-tauri/tauri.conf.json:8-10 devUrl http://localhost:1420 / frontendDist ../dist; the player window is index.html?floating-player=1, open_player_window in lib.rs:682-686), so they share one localStorage b…
- **Fix (refined):** Make both windows converge on the shared localStorage instead of relying on stale in-memory copies. Minimal fix in src/lib/store/layout.ts: after the create(persist(...)) call, add a window "storage" listener that rehydrates the store when the ytm-layout key changes in another window, e.g. `if (typeof window !== "undefined") { window.addEventListener("storage", (e) => { if (e.key === "ytm-layout") void useLayoutStore.persist.rehydrate(); }); }`. This makes the main window pick up the floater's floatingPinned:true before setMode("right") runs, so the subsequent full-state persist keeps the pinned flag. (Alternative, equally valid: route the pin toggle through a Tauri event to the main window…
- The pin toggle lives in the floating window (floating-player-app.tsx:95,118) which writes {mode, floatingPinned: true} to localStorage. The main window hydrated its own in-memory copy at app launch (floatingPinned: false) and never observes storage events. Failure scenario — the exact 'close/reopen cycle' the doc comment on line 9 promises to survive: user pins the floating player (floating window persists true), then closes it with the X button; Rust emits `player-window-closed`, the main window's handler (app-shell.tsx:115) calls setMode("right"), and persist writes the main window's full state including its stale floatingPinned: false — the pin is lost, and the next floating-window open is unpinned. Any setMode call in the main window while/after the floating window changed the pin has the same effect.
- **Fix:** Route the pin toggle through a Tauri event to the main window (like the playback/track-source bridges), or listen to the `storage` event / use zustand's cross-tab sync so both windows converge; alternatively store floatingPinned under its own key written only by the floating window.

### [35] 🔧 Shuffle flag is ignored when a new queue is set: setQueue/playNow leave the queue in linear order while the shuffle toggle shows active
- **MEDIUM / bug** — `src/lib/store/playback.ts:143`
- **🔧 Fixed (branch fix/review-p0):** setQueue/playNow apply shuffle.
- **Verdict:** CONFIRMED / high — Verified end-to-end in src/lib/store/playback.ts. setShuffle (lines 360-372) shuffles the tail and persists shuffle:true. setQueue (143-156) sets queue:tracks verbatim, playNow (125-141) builds queue from [mapped,...extras] verbatim, and playShelfItems (158-170) maps items in original order then calls setQueue — none of the three ever reads s.shuffle. The only place the flag re-applies mid-life is next() at line 295 (fisherYates on the repeat==='all' wrap point). The persisted flag is confirmed at partialize (line 397). Reachability confirmed: user Play actions call playShelfItems/playNow from album.$id.tsx:82,91, playlist.$id.tsx:171,178, track-list.tsx:287,294, and track-context-menu.tsx:…
- **Fix (refined):** In src/lib/store/playback.ts, apply the shuffle flag when a new queue is installed. In setQueue (around lines 143-156), after computing i, if get().shuffle && tracks.length > 1, keep tracks[i] as the current track and Fisher-Yates the rest: e.g. const cur = tracks[i]; const rest = fisherYates(tracks.filter((_, k) => k !== i)); queue = [cur, ...rest]; index = 0. Do the equivalent in playNow (lines 125-141): when get().shuffle && extras?.length, use [mapped, ...fisherYates(extras.filter(t => t.videoId !== mapped.videoId))]. playShelfItems delegates to setQueue so fixing setQueue covers it (verify the startIndex/index bookkeeping after reordering).
- setShuffle(true) shuffles the tail of the queue that exists at toggle time, and the flag is persisted across launches. But setQueue, playNow, and playShelfItems never consult s.shuffle — they install the tracks verbatim. Concrete: user enables shuffle, later (same session or after relaunch, since shuffle is persisted) presses Play on an album -> it plays strictly in album order even though the shuffle button renders as on. The flag only has an effect again at the repeat-all wrap point in next().
- **Fix:** In setQueue/playNow, when get().shuffle is true, shuffle the tracks after the start track (keeping the clicked track first), mirroring setShuffle's semantics.

### [36] ✅ Liked songs are cached twice under unrelated query keys with no synchronization
- **MEDIUM / bug** — `src/routes/library.tsx:133`
- **Verdict:** CONFIRMED / high — Verified end-to-end. Two independent caches back the same LM playlist. (1) ["liked-songs"] is populated by fetchLikedSongs() -> fetchPlaylist("LM") which drains ALL continuations (src/lib/innertube/library.ts:54-58; src/lib/innertube/playlist.ts:182-213); it drives heart-fill membership in LikeDislikeButtons (src/components/shared/like-buttons.tsx:65-71) and useTrackMenuController (src/components/shared/track-context-menu.tsx:107-112). (2) The Library>Songs tab uses useInfiniteQuery on ["library","liked-songs-pages"] with fetchPlaylistFirstPage("LM")/fetchPlaylistContinuation (src/routes/library.tsx:132-140) — same LM playlist, second network path. All rating handlers only call qc.setQueryD…
- **Fix (refined):** In each rating mutation handler, also sync the infinite-query cache. In src/components/shared/like-buttons.tsx onLike (lines ~82-95) and src/components/shared/track-context-menu.tsx runLike (~133), runRemoveRating (~149), runDislike (~160): after qc.setQueryData(["liked-songs"], ...), add qc.invalidateQueries({ queryKey: ["library","liked-songs-pages"] }). Preferably, remove the duplicate network fetch by deriving the heart-fill membership Set from the infinite query's pages (library.tsx) instead of the separate full-drain ["liked-songs"] query so a single cache backs both surfaces.
- The heart-fill machinery (like-buttons.tsx, track-context-menu.tsx, settings.tsx) uses `["liked-songs"]` populated by a full-drain fetchLikedSongs(), while the Library → Songs tab uses an infinite query under `["library", "liked-songs-pages"]` fetching the exact same LM playlist. Liking or unliking a track optimistically patches only `["liked-songs"]`; the Library Songs tab keeps showing the old list (and vice versa: scrolling the Library tab never warms the heart-fill cache, so the same data is fetched twice over the network — the drain is documented elsewhere as "7+ continuation round-trips").
- **Fix:** Back both surfaces with one cache: either derive the membership set from the pages of the infinite query, or make the Library tab consume ["liked-songs"] and drop the parallel key.

## Low (36)

### [37] 🔧 Leftover debris in the project root: a mangled empty directory, a stray PNG, and a stale two-month-old dist/ build
- **LOW / quality** — `D:Code-Projectsytm-native.claude:1`
- **🔧 Fixed (branch fix/review-p0):** PARTIAL: the mangled directory and the stale dist/ are deleted; the stray PNG is left alone (not my file, nothing references it, decided not to delete it).
- **Triage:** confirmed. Confirmed: an empty directory + a stray PNG + a stale dist/ in the root
- The root contains an empty directory literally named `D:Code-Projectsytm-native.claude` (a Windows absolute path with separators stripped, created by misconfigured tooling around May 2), a stray `yt-music-logo-png.png` that belongs in public/ (the SVG variants are there), and a `dist/` output from May 3 that no longer matches the source (it predates two months of changes). Because the project isn't under git, .gitignore does nothing to hide dist/ or .tanstack/, so stale artifacts sit indistinguishable from source and can be mistaken for the current build.
- **Fix:** Delete the mangled directory and stale dist/; move or delete the root PNG; after git init the existing .gitignore handles the rest.

### [38] 🔧 README is badly stale: phase checklist claims only scaffolding is done and the documented layout lists directories that don't exist
- **LOW / quality** — `README.md:19`
- **🔧 Fixed (branch fix/review-p0):** The phase checklist and the layout tree are updated.
- **Triage:** confirmed. Confirmed by reading the README
- The "Phase status" section shows Phases 1-8 unchecked, yet the codebase implements essentially all of them (InnerTube client + cookie auth, artist/album/playlist pages, audio engine + MediaSession/SMTC, queue + radio autoplay, library + liked songs, persistent query cache + infinite continuations, tray + custom titlebar, settings). The "Project layout" block documents `components/player/` and `lib/audio/` directories that were never created (the code lives in components/layout/ and lib/audio-engine.ts). Anyone onboarding from the README gets a wrong map of a codebase whose real documentation lives only in inline comments and a Russian-language docs/plan.md.
- **Fix:** Update the phase checklist and layout tree to match reality, or replace the phase section with a short feature list and point to docs/plan.md.

### [39] ✅ Cookie jar falls back to plaintext storage on non-Windows platforms.
- **LOW / security** — `src-tauri/src/lib.rs:123`
- **Triage:** confirmed. Verified earlier (first run)
- secure_store::encrypt/decrypt under #[cfg(not(windows))] return the plaintext unchanged, so on any macOS/Linux build the Google/YouTube auth cookies (SAPISID, __Secure-1PSID — full account credentials) are written to accounts/<id>/cookies.enc in app_data_dir as cleartext. Any other user process, backup, or file-stealer malware running as the user can read complete Google session cookies from a predictable path. The comment acknowledges this as a FIXME. Currently gated to non-Windows so impact is limited while the project is Windows-only, but the file extension (.enc) and 'encrypted cookies' logging misleadingly imply protection that does not exist off-Windows.
- **Fix:** Before shipping any non-Windows build, wire encrypt/decrypt into the macOS Keychain / libsecret (or refuse to persist cookies) rather than writing plaintext; do not name the file .enc when it is not encrypted.

### [40] ✅ accounts.json is mutated via unsynchronized read-modify-write and non-atomic writes, allowing lost updates and index corruption
- **LOW / bug** — `src-tauri/src/lib.rs:212`
- **Triage:** confirmed. Verified earlier (switch_account RMW)
- update_account_meta, switch_account, remove_account, clear_cookies, and the start_login background task each do read_index -> mutate -> write_index with no lock; commands run concurrently on the multi-threaded runtime, so e.g. the login task's account-append racing a user-initiated switch_account silently drops one of the two writes (an account vanishes or the wrong account stays active). write_index also writes the file in place with no temp-file+rename, so a crash or power loss mid-write leaves truncated JSON; read_index's unwrap_or_default then reports a fresh signed-out state while the per-account cookie dirs are orphaned on disk.
- **Fix:** Guard index mutations with a managed tokio::sync::Mutex and write via temp file + rename.

### [41] 🟡 Login webview profile directories leak if the app exits while the sign-in window is open
- **LOW / quality** — `src-tauri/src/lib.rs:399`
- **Triage:** plausible (light triage, not verified deeply)
- start_login creates app_cache_dir/login-sessions/<id> per attempt and only the polling task removes it (on success or window close). If the user quits the app (tray Quit calls app.exit(0)) or it crashes while the login window is open, the task dies and the profile dir — containing live Google session cookies in the WebView2 store, outside the DPAPI-encrypted jar — persists on disk indefinitely; there is no startup sweep of login-sessions/.
- **Fix:** On startup (setup hook), remove_dir_all the login-sessions directory before any login can start.

### [42] ✅ get_cookie_header exposes full raw auth cookies to any script running in the webview.
- **LOW / security** — `src-tauri/src/lib.rs:611`
- **Triage:** confirmed. An accurate description of the by-design cookie exposure in the webview
- get_cookie_header returns the decrypted SAPISID/__Secure-1PSID cookie string to the webview via IPC, and tauri-plugin-http is built with the unsafe-headers feature so the webview can set the Cookie header on outbound requests to the capability-allowlisted hosts (which include third parties genius.com, itunes.apple.com, apic-desktop.musixmatch.com). This is by design for InnerTube, but it means any script that executes in the webview (a compromised npm dependency, a CSP bypass) can read the user's complete Google session and attach it to requests to those third-party hosts, leaking Google auth cookies off youtube.com/google.com. Exfiltration is partially constrained by the CSP connect-src, but the third-party hosts in the http capability are legitimate targets a cookie could be misdirected to.
- **Fix:** Keep auth cookies in Rust and forward them only for youtube.com/google.com requests (do not return raw cookies to JS); scope get_cookie_header so it can never return youtube/google cookies for third-party hosts, and consider dropping unsafe-headers if the InnerTube calls can be proxied through Rust instead.

### [43] 🔧 Failed downloads are negative-cached for 60 seconds: retries within the grace window get an instant 502 without re-attempting
- **LOW / bug** — `src-tauri/src/lib.rs:1365`
- **🔧 Fixed (branch fix/review-p0):** Immediate eviction of the map entry on failure (no 60s negative cache).
- **Triage:** confirmed. Confirmed by reading spawn_downloader (eviction 60s after complete)
- After a failed download, state.complete=true but the map entry lingers for the 60 s eviction grace period. A retry (user presses play again) finds the entry, skips the wait loop immediately, sees no .webm on disk, and returns BAD_GATEWAY — no new yt-dlp attempt is possible for up to 60 s. The justifying comment ("brief re-play stays in RAM, then falls back to on-disk ServeFile") is stale: there is no in-memory buffer in this code, so for the failure path the grace period is pure retry-blocking with no benefit.
- **Fix:** Remove the map entry immediately when the download fails; keep the 60 s grace only for the success path (or drop it entirely, since success is served from disk anyway).

### [44] ✅ Production CSP whitelists broad localhost/wildcard-port and ws:// origins.
- **LOW / security** — `src-tauri/tauri.conf.json:28`
- **Triage:** confirmed. The CSP really does contain http://127.0.0.1:*, localhost:*, ws://localhost:*
- The shipped CSP connect-src includes http://127.0.0.1:*, http://localhost:*, and ws://localhost:* (and img-src http://127.0.0.1:*). The wildcard ports and localhost/websocket entries are dev-server conveniences that remain in the production policy, widening the set of local origins the webview may connect to (any local port, plus arbitrary local websocket servers run by other processes). This weakens CSP as a defense-in-depth exfiltration/backchannel barrier for the cookie exposure noted above.
- **Fix:** Split dev vs production CSP: in production drop ws://localhost:* and http://localhost:*, and if feasible pin 127.0.0.1 to the actual stream-server port rather than a wildcard (or route stream/cover through the tauri asset protocol instead of an http origin).

### [45] 🔧 Global keydown handler blocks the Tab key window-wide, disabling all keyboard navigation including inside dialogs
- **LOW / quality** — `src/components/layout/app-shell.tsx:43`
- **🔧 Fixed (branch fix/review-p0):** Tab navigation is restored; focus is cleared on window blur; Space no longer swallows interactive elements.
- **Triage:** confirmed. Tab preventDefault+blur for any non-input target
- **⬆️ Candidate for medium:** a11y, keyboard navigation is broken in the main UI
- useGlobalShortcuts preventDefaults every Tab press (and blurs the active element) whenever focus is not in an input/textarea/select/contenteditable. This kills keyboard accessibility for the entire app: focus can never be moved onto buttons, sliders, queue rows (which have tabIndex=0 and keyboard handlers that are now unreachable), and inside Radix dialogs a keyboard user who lands on the Cancel button cannot Tab onward — the key is swallowed and focus is blurred. The stated goal (avoiding ghost Space activations on Alt+Tab return) is achievable without banning Tab, e.g. by blurring on window blur/focus events.
- **Fix:** Remove the Tab block; instead blur the active element in a window 'blur' event listener, and rely on the existing Space handler's preventDefault to stop ghost activations.

### [46] 🟡 DragSnapOverlay shows misleading zones during floating-window drags: 'pop-out' perimeter glow while already floating, and a bottom-bar glow for a drop that always produces 'right' mode
- **LOW / bug** — `src/components/layout/floating-player-sync.tsx:284`
- **Triage:** plausible (light triage, not verified deeply)
- The drag:floating-position handler feeds detectZone(x, y) into the same store the in-window cover drag uses. Two mismatches with what actually happens on drop: (1) whenever the floater is dragged anywhere outside the main window (the common case), detectZone returns 'out' and DragSnapOverlay renders the full-perimeter 'release here to spawn a floating window' glow on the main window — but the window is already floating, so the hint is meaningless; (2) if the floater is dropped over the main window's bottom edge, the overlay lights the bottom-bar snap strip, but docking always goes through player-window-closed → setMode('right') (app-shell.tsx:115), so the user gets the right card despite the bottom-zone affordance.
- **Fix:** For floating-window drags, map zones to what the drop actually does: suppress the 'out' perimeter glow, and either honor the bottom zone (set mode 'bottom' when the drop zone was bottom) or only light the zone corresponding to the revert target.

### [47] 🟡 A sticky manual source preference shows "No lyrics found" on tracks where other sources have lyrics
- **LOW / quality** — `src/components/layout/lyrics-view.tsx:102`
- **Triage:** plausible (light triage, not verified deeply)
- pref persists in localStorage across tracks and sessions. If the user once picks e.g. "genius" for a specific song, then every subsequent track that Genius lacks renders "No lyrics found" (activeSource's data is null) even while LRCLIB has synced lyrics sitting in queries.lrclib.data. The dropdown disables unavailable sources for selection but nothing falls back when the preferred source has nothing for the current track, and there is no UI hint that switching to Auto would produce lyrics.
- **Fix:** When queries[pref].data is null and the query is settled, fall back to best (optionally with a small "showing X — preferred source unavailable" note), or reset pref to auto per-track.

### [48] ✅ player-bar.tsx is a 653-line grab-bag module that other components import utilities back out of
- **LOW / quality** — `src/components/layout/player-bar.tsx:1`
- **Triage:** confirmed. Structurally true (a 653-line grab-bag)
- The file mixes a network-fetching data hook (useITunesCover), a pure util (formatTime), and three reusable widgets (SourceToggle, ProgressSlider, VolumeControl) alongside the actual PlayerBar layout; player-bar-bottom.tsx imports four of these back from it, making a layout component the de-facto shared library for the player subsystem. settings.tsx (655 lines: four cards + two formatters + list rows) has the same shape. This coupling means touching the player-bar layout risks rippling through the bottom bar and floating window, and code-split chunks for one surface drag in the other's code.
- **Fix:** Split into components/player/{progress-slider,volume-control,source-toggle}.tsx, hooks/use-itunes-cover.ts, lib/format.ts; carve settings.tsx cards into separate files.

### [49] 🟡 Seek-time tooltip sticks visible after releasing a slider drag outside the wrapper
- **LOW / bug** — `src/components/layout/player-bar.tsx:251`
- **Triage:** plausible (light triage, not verified deeply)
- While the thumb is dragged, Radix's pointer capture retargets pointer/mouse events to the thumb, so the wrapper's onMouseLeave never fires (and is explicitly skipped when scrub !== null). The effect at line 208 keeps hoverX/hoverTime in sync with scrub. If the user releases the drag with the cursor outside the wrapper (common when flick-seeking to the end), onValueCommit sets scrub to null but nothing clears hoverX/hoverTime, and no mouseleave will fire because the pointer already left during capture — the time bubble stays permanently rendered above the slider until the mouse re-enters and leaves again.
- **Fix:** Clear the hover state on commit: in onValueCommit also call setHoverX(null)/setHoverTime(null) (or clear them in the scrub-sync effect when scrub becomes null).

### [50] 🔧 Scroll-wheel unmute discards the stored volume, resetting it to 5%
- **LOW / bug** — `src/components/layout/player-bar.tsx:310`
- **🔧 Fixed (branch fix/review-p0):** Wheel-unmute keeps the volume.
- **Triage:** confirmed. Confirmed: current = muted ? 0 : volume → wheel-up drops the volume to 5%
- **⬆️ Candidate for medium:** users lose their volume setting often
- VolumeControl's onWheel computes `const current = muted ? 0 : volume`, so when the player is muted at, say, 60% volume, one wheel-up tick sets volume to max(0, min(1, 0 + 0.05)) = 0.05 and unmutes (setVolume clears muted). The user's 60% setting is destroyed and playback resumes at 5% — clicking the mute button instead correctly restores 60%. Wheel-down while muted also unmutes at volume 0, which contradicts the comment 'Unmutes on any change so the change is audible'.
- **Fix:** When muted, unmute and step from the stored volume: `const current = volume;` and only call toggleMute()/setVolume(next) with next derived from the real volume.

### [51] ✅ Entire player card re-renders on every position tick and on every pointermove while scrubbing
- **LOW / perf** — `src/components/layout/player-bar.tsx:387`
- **Triage:** confirmed. position is selected in the top-level selector → the whole card re-renders
- PlayerBar (and PlayerBarBottom identically) selects `position` in its top-level useShallow selector and keeps the `scrub` state at the top level, so the whole card — motion.div wrappers, 1024px Thumbnail, ArtistLinks, LikeDislikeButtons, LyricsBody wrapper, and the QueueBody overlay when open — reconciles ~4×/sec from timeupdate during playback, and at pointer-move rate (60-120 Hz) while the user drags the progress slider. Only ProgressSlider and the two timecode spans actually consume position/scrub. Not a crash, but it is the main per-frame re-render hotspot in the app and grows with queue length when the queue overlay is open (QueueBody re-renders all rows).
- **Fix:** Move position/scrub consumption into a small child component (e.g. a ProgressSection that itself subscribes to position and owns scrub state), so the card body only re-renders on track/status changes.

### [52] 🔧 'Add to playlist' submenu falsely shows 'No playlists yet.' for keyboard/touch users because the fetch is only primed on pointerenter
- **LOW / bug** — `src/components/shared/track-context-menu.tsx:289`
- **🔧 Fixed (branch fix/review-p0):** Prime the playlists on onFocus (keyboard).
- **Triage:** plausible (light triage, not verified deeply). a11y, the submenu only loads on pointerenter
- The user-playlists query is `enabled: false` and only fetched via primeUserPlaylists on SubTrigger's onPointerEnter. Opening the submenu with keyboard navigation (ArrowRight/Enter on the sub trigger, standard in Radix menus) never fires pointerenter, so playlists.data stays undefined and isFetching stays false — the render branch `(playlists.data ?? []).length === 0` then shows 'No playlists yet.', telling a signed-in user with playlists that they have none.
- **Fix:** Also prime on the SubTrigger's focus/onSelect/onOpenChange (or fetch when the SubContent mounts), and distinguish 'not yet loaded' (data undefined) from 'loaded empty' (data.length === 0) in the render branches.

### [53] ✅ Three near-identical time formatters (plus byte/relative formatters) live inline in components with subtly different semantics
- **LOW / quality** — `src/components/shared/track-list.tsx:36`
- **Triage:** confirmed. A structural duplicate of the formatters
- `formatDuration` is duplicated character-for-character in track-list.tsx:36 and queue-panel.tsx:29 (returns "—" for 0/NaN, no hours handling — a 1h05m track renders as "65:03"), while player-bar.tsx:82 exports a third variant `formatTime` (returns "0:00", floors seconds). settings.tsx additionally defines private formatBytes/formatRelative. The same on-screen duration can render differently between a track row and the player bar, and a fix to one copy (e.g. adding h:mm:ss) won't propagate.
- **Fix:** Create src/lib/format.ts with formatDuration/formatTime/formatBytes/formatRelative and import everywhere.

### [54] ✅ Every virtualized track row mounts two full menu controllers, two liked-songs query observers, and two NewPlaylistDialog/Radix roots
- **LOW / perf** — `src/components/shared/track-list.tsx:415`
- **Triage:** confirmed. Per-row TrackContextMenu + TrackMoreMenu + LikeDislikeButtons
- **⬆️ Candidate for medium:** perf in the most-rendered component (60+ observers/dialogs)
- TrackRow wraps itself in TrackContextMenu and also renders TrackMoreMenu; each calls useTrackMenuController, which registers a useQuery observer on ["liked-songs"] and ["user-playlists"] and renders its own NewPlaylistDialog. With ~25 visible rows + overscan that is ~60+ query observers and ~60 dialog instances that are created and destroyed continuously as the virtualizer mounts/unmounts rows during scroll — measurable observer subscribe/unsubscribe churn on long playlists (the module-level liked-id Set memo only mitigates the membership scan, not observer churn).
- **Fix:** Hoist a single menu controller + one shared NewPlaylistDialog to the TrackList level (pass the target item via state), or lazily create the controller only when a menu actually opens.

### [55] 🔧 The youtubei.js Innertube singleton is dead code — getInnertube/getMusic have no callers
- **LOW / quality** — `src/lib/innertube/client.ts:76`
- **🔧 Fixed (branch fix/review-p0):** youtubei.js is gone, the settings chunk went 277KB→12KB and the class parsers are out of the bundle.
- **Triage:** confirmed. Duplicate of high #12, confirmed
- All API traffic goes through the raw tauriFetch POSTs in shared.ts; the only import from client.ts anywhere in src/ is resetInnertube (settings.tsx, accounts.ts, app-sidebar.tsx), which matters only for resetAuthCache(). The Innertube client, its cookie loader, and the fetch wrapper never execute, yet importing client.ts statically pulls the entire youtubei.js library into the bundle, and the file's doc comments ("so library / liked-songs endpoints work") describe behavior that does not exist. This also makes the module misleading for maintenance: someone fixing auth will reasonably (and wrongly) look here first.
- **Fix:** Delete the Innertube creation path (or lazy-load it if genuinely planned), keep resetInnertube as a thin re-export of resetAuthCache, and drop the youtubei.js dependency if nothing else uses it.

### [56] 🔧 Shelf ids can collide across infinite-feed pages, producing duplicate React keys
- **LOW / bug** — `src/lib/innertube/home.ts:43`
- **🔧 Fixed (branch fix/review-p0):** Shelf id from a hash of the full token, so no collisions.
- **Triage:** plausible (light triage, not verified deeply)
- The id is `${cursor.slice(0,12)}-${title}${suffix}`, but browse continuation tokens for the same feed share their leading characters (protobuf/base64 prefix like "4qmFsgL…"), so cursorTag is effectively constant across pages, and the titleSeen de-dup counter is per-page only. Scenario: two continuation pages each contain a header-less shelf (mapShelfWrapper falls back to "Section 1") or a repeated shelf title → identical shelf.id values in the flattened list rendered with key={shelf.id} (src/routes/index.tsx:77) → React duplicate-key warnings and shelves rendering/reconciling incorrectly. Same code in explore.ts:40-57.
- **Fix:** Include a page ordinal (pageParam index) or a hash of the full cursor in the id instead of cursor.slice(0,12).

### [57] 🔧 Track-count regex breaks on thousands separators: "5,000 songs" parses as trackCount 0
- **LOW / bug** — `src/lib/innertube/playlist.ts:107`
- **🔧 Fixed (branch fix/review-p0):** The count regex handles the thousands separator (+album.ts).
- **Triage:** confirmed. Confirmed: /(\d+)\s+songs?/ on "5,000 songs" → 0
- secondText.match(/(\d+)\s+songs?/i) cannot span the comma, so it matches the digits after the last separator: "5,000 songs" → 0, "1,234 songs" → 234. Same pattern in album.ts:67. Scenario: any playlist/album whose header count uses a locale thousands separator (all >999-track playlists in en-US) shows a wildly wrong or zero song count in the entity header.
- **Fix:** Strip separators first: secondText.match(/([\d,. \s]*\d)\s+songs?/i) then parseInt(m[1].replace(/[^\d]/g, "")).

### [58] 🔧 In-flight cookie load can repopulate the cache with stale credentials after resetAuthCache()
- **LOW / bug** — `src/lib/innertube/shared.ts:119`
- **🔧 Fixed (branch fix/review-p0):** Epoch guard: an in-flight cookie load no longer overwrites the cache after a reset.
- **Triage:** plausible (light triage, not verified deeply)
- **⬆️ Candidate for medium:** auth correctness, stale cookies for 5 min after a reset (narrow window)
- loadCookieHeader's then-handler unconditionally writes cookieCache and nulls cookiePromise when the invoke resolves. If resetAuthCache() runs while a load is in flight (sign-in/sign-out completing while any query is fetching cookies), the pre-reset value — e.g. the empty pre-login jar — is written back after the reset and served for up to COOKIE_CACHE_TTL_MS (5 min): library/liked requests go out anonymous right after a successful sign-in until the TTL expires. The handler can also null out a newer cookiePromise created after the reset.
- **Fix:** Capture the promise identity (const p = cookiePromise = …) and in the handler only commit if cookiePromise === p (generation/epoch check).

### [59] 🟡 Column classifier misfiles nav-less artist/album text into dateAdded/playCount
- **LOW / bug** — `src/lib/innertube/shared.ts:646`
- **Triage:** plausible (light triage, not verified deeply)
- In mapResponsiveListItem, a flex column whose runs carry no browseEndpoint falls through to the counter/date heuristics. looksLikeDate matches any word starting with a month token, so an artist or album named "Mario Judah", "May Wells", or Russian "Декабрь" in a nav-less column (common for uploaded/private tracks and some compilation rows) is stored as dateAdded and shown in the playlist "Added" column; a bare "2024" year column matches the compact-number regex and becomes playCount. Wrong metadata is displayed silently.
- **Fix:** Only run the date/counter heuristics on columns past the known artist/album positions, or require the date regex to include a digit for month-name matches.

### [60] 🔧 HTTP-level 401/403 responses are swallowed by `if (!r.ok) return null` so the token-invalidate-and-retry path only triggers on envelope status_code 401
- **LOW / bug** — `src/lib/lyrics/musixmatch.ts:203`
- **🔧 Fixed (branch fix/review-p0):** HTTP 401/403 → auth failure (the token retry fires).
- **Triage:** plausible (light triage, not verified deeply). Matches the if(!r.ok) return null pattern
- findTrackId, getSubtitle, and getPlainLyrics all return plain null when r.ok is false, before the envelope status_code check on the next line can run. The auth-failure retry in fetchMusixmatchLyrics (lines 149-154) therefore only fires when Musixmatch returns HTTP 200 with message.header.status_code === 401. If the server rejects a stale/flagged token at the HTTP layer instead (real 401/403 responses do occur when the apic-desktop gateway flags a token), the cached token is kept and every Musixmatch lookup silently returns null until the 9-minute TTL expires — lyrics missing from this source for up to 9 minutes across all tracks with no retry attempted. Related: fetchToken never inspects the token.get envelope's status_code either, relying solely on user_token absence.
- **Fix:** In each request helper, treat r.status === 401 (and arguably 403) as "auth-failure" before the generic !r.ok bailout.

### [61] 🔧 The LRC [offset:±ms] global-shift tag is silently discarded, mis-syncing files that carry it
- **LOW / bug** — `src/lib/lyrics/parse-lrc.ts:17`
- **🔧 Fixed (branch fix/review-p0):** [offset:±ms] is applied.
- **Triage:** confirmed. Confirmed: [offset:±ms] does not match tsRe, the line is dropped
- The parser only matches numeric [mm:ss.xx] stamps; metadata tags are intentionally skipped, but that includes the standard [offset:+500] / [offset:-500] tag which mandates shifting every timestamp. LRCLIB is a user-contributed database, so uploaded LRC bodies can legitimately carry an offset tag; for such a file every highlighted line is early/late by the offset amount (e.g. half a second), which is quite visible with the 0.72 s lookahead cross-fade tuning in lyrics-view.
- **Fix:** Scan for /\[offset:([+-]?\d+)\]/i once per document and add offset/1000 (LRC offset is milliseconds, positive = shift timestamps earlier) to each parsed start.

### [62] 🔧 Comment claims primary-pointer requirement but e.isPrimary is never checked; concurrent pointers stack duplicate listeners and clobber drag state
- **LOW / quality** — `src/lib/player-drag.ts:125`
- **🔧 Fixed (branch fix/review-p0):** Added the e.isPrimary check.
- **Triage:** plausible (light triage, not verified deeply)
- The comment says 'ignore touch/pen ambiguity by also requiring primary pointer', but the code only checks e.button !== 0 (which is 0 for touch contacts). A second simultaneous pointerdown (multi-touch on a touchscreen laptop) passes the guard, overwrites startRef/draggingRef shared by the first drag, and attaches a second set of move/up listeners to the same element; the first pointer's cleanup then resets the shared refs mid-drag for the second pointer, producing flickering snap zones and possible unintended layout-mode switches on release.
- **Fix:** Add `if (!e.isPrimary) return;` (matching the comment) or track per-pointerId state.

### [63] ✅ Persisted query cache is not keyed by account, so account isolation depends entirely on an in-session clear racing a 1 s throttled write
- **LOW / bug** — `src/lib/query-client.ts:22`
- **Triage:** confirmed. The root of account isolation, matches high #8
- The persister uses one global key with throttleTime 1000. Account isolation is achieved only by `qc.clear()` inside the accounts-changed listener (src/lib/store/accounts.ts:129) followed by the throttled persist of the now-empty cache. Failure scenario: user switches from account A to B and quits the app within the throttle window (or before the empty/refetched state is flushed) — localStorage still holds account A's home feed, playlists, and liked-songs; the next launch hydrates and renders A's private data under B's session until background refetches (which may fail offline) replace it, and liked-heart fills come from A's liked-songs list. Narrow window in isolation, but it is the same design gap that the floating-window finding exploits with a much wider window.
- **Fix:** Include the active account id in the persister key (buster or key suffix) so a mismatched cache is discarded at restore time instead of relying on in-session clearing.

### [64] 🔧 shouldPersistQuery whitelists head "playlist" but the playlist route keys its data as "playlist-pages", so playlists are never persisted
- **LOW / bug** — `src/lib/query-client.ts:37`
- **🔧 Fixed (branch fix/review-p0):** The persist whitelist is corrected to "playlist-pages".
- **Triage:** confirmed. The persist half of high #9, confirmed
- The persistence whitelist checks `head === "playlist"`, yet the only playlist data query in the app is ["playlist-pages", id] (playlist.$id.tsx:54) and the library liked-songs list is ["library", "liked-songs-pages"]. Neither matches, so the stated goal (instant hydration of previously visited playlists on cold start) silently never happens — every playlist page cold-starts on the skeleton and refetches, while the dead "playlist" entry suggests it works.
- **Fix:** Change the whitelist entry to "playlist-pages" (mind the 500 KB per-query budget for long playlists) or remove the dead entry.

### [65] ✅ Persistence pipeline runs full JSON.stringify per persistable query on every dehydrate, plus a whole-cache stringify to localStorage, on the UI thread up to once per second
- **LOW / perf** — `src/lib/query-client.ts:69`
- **Triage:** confirmed. JSON.stringify on the UI thread during dehydrate
- shouldDehydrateQuery calls fitsInPersistBudget (JSON.stringify of each candidate query's data, up to 500 KB each) every time the sync persister dehydrates, and createSyncStoragePersister then stringifies the entire dehydrated cache synchronously into localStorage (throttleTime 1000). During infinite-scroll of Home (persisted, multi-page) with several album/artist/liked-songs entries also cached, this is repeated multi-hundred-KB synchronous serialization every second while the cache mutates — main-thread jank precisely while the user is scrolling. localStorage quota overrun also fails silently (no retry strategy configured), stopping persistence without any signal.
- **Fix:** Use the async persister (@tanstack/query-async-storage-persister) with an idle-callback serializer, cache the budget check per query dataUpdatedAt instead of re-stringifying, and consider a retry/removal strategy for quota errors.

### [66] ✅ Account switch permanently destroys the previous account's search history instead of bucketing it per account
- **LOW / quality** — `src/lib/store/accounts.ts:117`
- **Triage:** confirmed. clear() on the shared ytm:search-history on account switch
- `useSearchHistory.getState().clear()` wipes the single persisted `ytm:search-history` list on every accounts-changed event. Failure scenario: a user with two accounts who switches back and forth loses all typed-search history on every switch — switching A→B→A leaves A with an empty history despite the store being persisted specifically to survive sessions. This is inconsistent with pinned-playlists, which solved the identical problem with a per-account `byAccount` map in the same codebase.
- **Fix:** Adopt the pinned-playlists pattern: key history lists by account id and swap the active bucket on switch instead of clearing.

### [67] 🟡 byAccount buckets are orphaned forever when an account is removed, and pins are unrecoverable on re-add because account ids are regenerated
- **LOW / quality** — `src/lib/store/pinned-playlists.ts:38`
- **Triage:** plausible (light triage, not verified deeply)
- removeAccount (src/lib/store/accounts.ts:222) deletes the account on the Rust side only; nothing prunes `byAccount[id]`. Rust ids are `acct-<timestamp-nanos>` generated fresh per sign-in (src-tauri/src/lib.rs:301), and email-based dedup only maps back to an existing entry — so after remove + re-login of the same Google account a NEW id is issued. Failure scenario: user removes an account and signs back into the same Google account; their pinned playlists do not come back (parked under the dead old id), and the dead bucket persists in localStorage indefinitely. Low stakes (small data), but both data loss and leftover garbage are permanent.
- **Fix:** Prune the bucket in a removeAccount wrapper (or on accounts-list reconciliation), and/or key buckets by email once meta backfill resolves it.

### [68] 🔧 resetStreamBaseUrl is a dead export, meaning the failure-recovery path it documents does not actually exist
- **LOW / quality** — `src/lib/stream.ts:46`
- **🔧 Fixed (branch fix/review-p0):** Removed the dead resetStreamBaseUrl export.
- **Triage:** confirmed. Confirmed by grep, the only reference is the definition itself
- The function's doc comment says to call it "when the localhost stream server appears unreachable (e.g. after a Rust crash + restart that bound a different port)", but nothing in the codebase ever calls it. If the axum server ever rebinds, `baseUrlPromise` stays resolved to the dead port and every subsequent track errors until full app restart — the escape hatch was built and never wired to any error handler (e.g. the MEDIA_ERR_NETWORK branch in audio-engine.ts).
- **Fix:** Either wire it into the audio-engine error path (reset + retry once on network media errors) or delete the export so the dead recovery story isn't misleading.

### [69] ✅ Premium detection race at launch: the first track is always streamed with ?ephemeral=1 for Premium users, bypassing the persistent cache
- **LOW / bug** — `src/lib/stream.ts:51`
- **Triage:** confirmed. Confirmed: ephemeralSuffix() reads isPremium() synchronously
- ephemeralSuffix() reads isPremium() synchronously at URL-build time, but usePremiumStore.status starts as null every launch and only flips to "premium" after an async innertube round-trip. Meanwhile AudioEngine eagerly resolves the persisted queue's current track at mount (and any track the user plays in the first seconds). Result for Premium users: the launch track is routed to the ephemeral dir (wiped on every startup), never lands in the persistent library cache, and gets fully re-downloaded on a later play; prefetchStream is likewise skipped until the status flips. The stream URL is never re-resolved when premium flips mid-track since the engine effect doesn't subscribe to premium state.
- **Fix:** Re-resolve the current stream URL when premium status transitions (subscribe in the engine), or defer the ephemeral decision to the Rust side / to request time instead of baking it into the cached URL.

### [70] 🔧 prefetchStream marks a videoId as warm even when the server responds with an error status
- **LOW / bug** — `src/lib/stream.ts:79`
- **🔧 Fixed (branch fix/review-p0):** prefetch no longer marks warm on an HTTP error (res.ok).
- **Triage:** confirmed. Confirmed: fetch does not reject on 4xx/5xx, res.ok is not checked
- fetch() only rejects on network-level failures; an HTTP 4xx/5xx from the local prefetch handler (e.g. yt-dlp spawn failure, transient extractor error) resolves normally, so the id stays in the `prefetched` Set and is never retried for the rest of the session. The next 'next' press then pays the full ~2 s cold-start cost the prefetch was meant to hide, and the miss silently repeats for every affected track.
- **Fix:** Check res.ok and delete the id from the Set on non-2xx responses, same as the catch branch.

### [71] ✅ Infinite-scroll machinery and error/sentinel UI are copy-pasted across routes, and the /next playlist-panel extraction is duplicated in the API layer
- **LOW / quality** — `src/routes/playlist.$id.tsx:102`
- **Triage:** confirmed. A structural duplicate (matches medium #10/#11)
- playlist.$id.tsx and library.tsx's LikedSongsView duplicate the same `AnyPage` union, first-page/continuation queryFn dispatch, IntersectionObserver sentinel effect (identical 600px rootMargin, same isFetchingNextPage guard), and the same sentinel/loading JSX; the destructive-red error panel is likewise duplicated (library.tsx ErrorCard vs playlist.$id.tsx inline). Separately, the deep `contents.singleColumnMusicWatchNextResultsRenderer...playlistPanelRenderer.contents` extraction + mapPlaylistPanelVideo loop appears verbatim in both src/lib/innertube/radio.ts:19-30 and src/lib/innertube/playlist.ts:126-137. Each copy is a place for YT payload-shape fixes to land in only one spot.
- **Fix:** Extract a `useInfinitePlaylist(queryKey, playlistId)` hook + shared <LoadMoreSentinel/> and <ErrorPanel/> components; extract an `extractPlaylistPanelTracks(json)` helper in the innertube layer.

### [72] ✅ Type safety has a deliberate hole: noUncheckedIndexedAccess is off and the entire InnerTube layer is typed as Record<string, any>
- **LOW / quality** — `tsconfig.json:17`
- **Triage:** confirmed. Confirmed: YtNode = Record<string, any> (shared.ts:5), the flag is off
- `YtNode = Record<string, any>` (src/lib/innertube/shared.ts:5) makes the 900-line parser file and every downstream fetcher effectively unchecked — typos in renderer key paths (the exact class of bug this code is most prone to) compile silently, and `any` leaks into consumers (e.g. `Primitives` in track-context-menu.tsx is ComponentType<any> x5). With noUncheckedIndexedAccess off, patterns like `parts[0] * 60 + parts[1]` and `thumbnails[0]` in strict-mode files also skip undefined checks. Given there is no linter or test suite either, the compiler is the only safety net and it has been opted out of exactly where payloads are least trustworthy.
- **Fix:** Enable noUncheckedIndexedAccess (fix fallout incrementally); consider `unknown`-based narrowing helpers or a lightweight schema check at the innertubePost boundary instead of blanket any.

## Assessment by subsystem

- Strengths: the stream server correctly binds to the 127.0.0.1 loopback (not 0.0.0.0) on a random port; video_id and cover filename inputs are tightly sanitized (alnum/_/- , length caps, explicit ".." rejection) so path traversal is well-guarded; all external processes (yt-dlp) are spawned with argument arrays and no shell, so there is no command injection; cookies are encrypted with DPAPI on Windows and yt-dlp is deliberately never handed cookies; the Tauri capability http scope is host-limited and the CSP is otherwise restrictive (script-src 'self', object-src/frame-ancestors/form-action 'none'). Weaknesses: the local HTTP server pairs CorsLayer::permissive() with a complete absence of Origin/Host/auth validation on endpoints that spawn processes and write unbounded amounts to disk, making it exposed to CSRF/DNS-rebinding abuse; cache_cover is an unrestricted server-side URL fetch (SSRF primitive); cookie storage silently degrades to plaintext off-Windows; and raw auth cookies are handed to the webview while unsafe-headers is enabled, so any webview compromise yields full Google credentials. Overall the file system / process boundaries are handled carefully, but the network-facing trust boundary (who may talk to the local server, and what URLs it may fetch) is the weak point.

- The Rust backend is well above hobby-project average: thorough doc comments explaining non-obvious decisions (moov-atom streaming rationale, cookie-domain normalization, event dedup), consistent String-error handling with essentially no unwraps on untrusted data, correct input sanitization on both proxy routes (video-id and cover-filename validation rule out traversal), and thoughtful touches like DPAPI cookie encryption, magic-byte MIME sniffing, and the 32 KB stub-download floor. Its weaknesses cluster in concurrency and process lifecycle: check-then-act races on the downloads map and accounts index, no timeout/kill discipline on yt-dlp children, one blocking subprocess call on the main thread, and a completely unauthenticated permissive-CORS localhost server. Nothing here is architecturally unsound — the fixes are all local — but the absence of tests means none of these race/failure paths has ever been exercised deliberately.

- This is a well-crafted subsystem overall: each source is cleanly isolated behind a common Lyrics type, comments explain non-obvious decisions (CORS/User-Agent constraints, LRCLIB get-vs-search race, Musixmatch UpgradeOnly token rejection), the LRC parser handles multi-timestamp lines and 1/2/3-digit fractions correctly, and track-change races are soundly delegated to react-query key identity — I found no data races. The two systemic weaknesses are error handling that collapses every failure into null (defeating retry and caching \"no lyrics\" answers that are really network errors) and trust in remote search relevance without validating the match, which for Genius produces confidently displayed wrong-song lyrics. The time-sync highlight math in lyrics-view is correct at boundaries (linear scan, not binary search, but fine at lyric scale); its real defect is scroll-state lifecycle (no centering on mount, no reset on track change) rather than the sync logic itself.

- Overall this is well-crafted code: comments explain intent and known hazards, there are deliberate race guards (resolve token for stream resolution, StrictMode-safe Tauri listener disposal, persist partialize that never auto-plays on launch), listener/cleanup hygiene is solid, and the floating-window remote-control bridge is a thoughtful design. The main structural weakness is that the store-to-engine contract communicates track transitions through derived strings (videoId) and a boolean `playing` rather than explicit transition events, which is exactly what breaks repeat-one, duplicate-videoId advancement, and the paused-start blip — the store thinks a transition happened while the engine's effects see no dependency change. Async side effects that lack the staleness guards the stream resolver has (auto-radio append, premium detection) form the second cluster of bugs. With no tests, these mode-interaction regressions (shuffle x setQueue, repeat-one x ended) have nothing to catch them.

- Overall this is well-crafted UI code: the async Tauri listen() teardown races are handled consistently with cancelled-flag patterns, the transport/queue snapshot split shows real attention to IPC cost, comments explain non-obvious decisions honestly, and the entity header does composited-property animation correctly. The weak spot is the floating-player state machine: the action-override bridge covers only part of the store surface (menu actions silently break in the floater), and the window-close event conflates user intent with programmatic closes, so layout mode gets corrupted by ordinary queue operations. The dock watcher's timeout-as-drop heuristic and the overlay-zone semantics for floating drags are both approximations that misbehave in reachable states. Smaller polish issues (DnD off-by-one, wheel-unmute, global Tab block) suggest limited testing of secondary interaction paths — unsurprising given there are no tests at all.

- This is an unusually well-crafted persistence layer for an untested codebase: stores are small and single-purpose, persist configs deliberately partialize volatile fields (premium status rederived per launch, playback position never auto-resumes), versioned migrations and a legacy-bucket handoff exist for pinned playlists, selectors use stable EMPTY references, all Tauri listeners are StrictMode-safe, and hydration is race-free because everything uses synchronous localStorage rather than the async tauri-plugin-store. Write amplification is well controlled (search history writes only on submit, sort modes drop default entries, track-source caps its map). The systemic weakness is multi-window awareness: the main and floating windows are independent JS contexts sharing localStorage keys with last-writer-wins semantics, which breaks both the query-cache persister (including a cross-account data resurrection path) and the floatingPinned preference. Account isolation is done by in-session clearing of shared buckets rather than account-scoped keys, which is fragile at shutdown boundaries and lossy for search history; caches with lazy TTL eviction (cover art) also grow without bound.

- Overall this is a carefully written hand-rolled InnerTube layer: parsing is defensive (optional chaining everywhere, tree-walk fallbacks with WeakSet cycle guards, synthesized i.ytimg thumbnails), pagination has real safety rails (token-echo check, 200-page cap, dedupe sets), and the comments documenting YT response-shape quirks are unusually good. The client-init race the module structure suggests is moot because the youtubei.js singleton is never actually used — which is itself the problem: dead dependency plus misleading docs. The main weaknesses are on the write path (mutations trust HTTP 200 and invalidate query keys that don't exist, so optimistic UI can lie) and in copy-paste drift (home/explore extraction helpers duplicated verbatim, readRun vs readRuns already diverged); the heuristic column classifiers and count regexes will keep producing occasional silently-wrong metadata as YT varies locales and layouts.

- This is a well-crafted UI layer overall: the virtualized TrackList handles scrollMargin, measurement, and duplicate-id keys correctly; the Thumbnail fallback chain (iTunes override → disk-cached upgrade → largest API variant, with per-tier onError demotion and cancellation-safe async resolve) is genuinely careful; routes consistently have skeleton/error states and search params are properly validated. The main weaknesses are query-key hygiene drift — three different keys hold liked/playlist track data ("liked-songs", "library/liked-songs-pages", "playlist-pages") with no cross-synchronization, producing the dead invalidation and dead persistence entry found above — and infinite-scroll error handling, which either retries unboundedly or wipes loaded content on a single failed continuation. There are also DOM-side channels (data-videoid querySelector) that were designed before virtualization and now track detached nodes. No tests exist, so none of these regressions would be caught automatically.

- This is an unusually thoughtful hobby codebase: the frontend architecture is genuinely strong, with disciplined layering (raw InnerTube fetch + parsers in lib/innertube, Zustand stores with careful persist/partialize choices, a well-designed cross-window bridge that splits high-frequency transport from low-frequency queue snapshots), pervasive high-quality comments explaining *why* (StrictMode listener races, CORS/CSP workarounds, cache TTL rationale), and consistent per-layer error strategies (best-effort null for lyrics/covers, throw for InnerTube, toast at the UI edge). The weaknesses are all about the absence of guardrails rather than the code itself: no git, no lint, no tests, and no CI means the ad-hoc query-key strings have already drifted (dead invalidations, dead persist whitelist entries), a fully dead youtubei.js client still ships ~270 KB, and verbatim duplications (source-switch flow, infinite-scroll plumbing, time formatters) have started accumulating in the component layer where only convention prevents divergence.
